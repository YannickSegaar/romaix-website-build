---
phase: 07-case-studies-detail-pages
plan: 03
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - src/components/case-study/CaseStudyCard.tsx
  - src/components/case-study/MetricsDisplay.tsx
  - src/components/case-study/TestimonialBlock.tsx
  - src/components/case-study/WorkflowComparison.tsx
  - src/components/case-study/RelatedCaseStudies.tsx
  - src/components/case-study/IndustryFilter.tsx
  - src/components/case-study/index.ts
autonomous: true

must_haves:
  truths:
    - "CaseStudyCard displays metric value prominently with client and industry"
    - "MetricsDisplay renders results array with before/after comparison"
    - "TestimonialBlock shows quote with attribution"
    - "WorkflowComparison shows side-by-side before/after images"
    - "RelatedCaseStudies renders list of related study cards"
    - "IndustryFilter provides navigation links for filtering"
  artifacts:
    - path: "src/components/case-study/CaseStudyCard.tsx"
      provides: "Card component for listing grid"
      exports: ["CaseStudyCard"]
    - path: "src/components/case-study/MetricsDisplay.tsx"
      provides: "Results metrics grid"
      exports: ["MetricsDisplay"]
    - path: "src/components/case-study/TestimonialBlock.tsx"
      provides: "Client testimonial quote block"
      exports: ["TestimonialBlock"]
    - path: "src/components/case-study/WorkflowComparison.tsx"
      provides: "Before/after workflow images"
      exports: ["WorkflowComparison"]
    - path: "src/components/case-study/RelatedCaseStudies.tsx"
      provides: "Related content recommendations"
      exports: ["RelatedCaseStudies"]
    - path: "src/components/case-study/index.ts"
      provides: "Barrel export for all components"
      exports: ["CaseStudyCard", "MetricsDisplay", "TestimonialBlock", "WorkflowComparison", "RelatedCaseStudies", "IndustryFilter"]
  key_links:
    - from: "src/components/case-study/CaseStudyCard.tsx"
      to: "/case-studies/[slug]"
      via: "Link href"
      pattern: "href=.*case-studies/"
    - from: "src/components/case-study/MetricsDisplay.tsx"
      to: "CaseStudyMetadata.results"
      via: "results prop"
      pattern: "results.*MetricResult"
---

<objective>
Create case study UI components for detail and listing pages

Purpose: Reusable components for displaying case study content with visual hierarchy
Output: 6 components with barrel export following blog component patterns
</objective>

<execution_context>
@/Users/yrs/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yrs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-case-studies-detail-pages/07-RESEARCH.md

# Schema types from Plan 01
@src/lib/schemas/case-study.ts

# Reference patterns from blog
@src/components/blog/BlogCard.tsx
@src/components/blog/CategoryFilter.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CaseStudyCard and IndustryFilter components</name>
  <files>src/components/case-study/CaseStudyCard.tsx, src/components/case-study/IndustryFilter.tsx</files>
  <action>
**CaseStudyCard.tsx:**
Create card for listing grid with explicit props (not single object - follow BlogCard pattern):

Props: slug, title, client, industry, metric, metricValue, description

Structure:
- Card with hover:shadow-lg transition (match homepage cards)
- Metric value prominently displayed (text-4xl md:text-5xl font-bold text-primary)
- Metric name below value
- Client and industry as CardDescription
- Description text
- "Read case study" link with ArrowRight icon and hover animation

Import from @/components/ui/card, Link, ArrowRight from lucide-react.
Add 'use client' only if using client hooks (likely not needed for this card).

**IndustryFilter.tsx:**
Create filter navigation mirroring CategoryFilter from blog:

Props: industries: string[], selectedIndustry?: string

- 'use client' directive (uses useRouter for navigation)
- Horizontal flex layout with gap-2
- "All" button + button per industry
- Active state: bg-primary text-primary-foreground
- Inactive state: hover:bg-muted
- Uses router.push with searchParams for filtering
- Preserves other query params if any
  </action>
  <verify>npx tsc --noEmit src/components/case-study/CaseStudyCard.tsx src/components/case-study/IndustryFilter.tsx</verify>
  <done>Both components compile, CaseStudyCard links to /case-studies/[slug]</done>
</task>

<task type="auto">
  <name>Task 2: Create MetricsDisplay and TestimonialBlock components</name>
  <files>src/components/case-study/MetricsDisplay.tsx, src/components/case-study/TestimonialBlock.tsx</files>
  <action>
**MetricsDisplay.tsx:**
Display results array with visual hierarchy:

Props: results: Array<{ metric: string; before: string; after: string; improvement: string }>

Structure:
- Grid layout: md:grid-cols-2 lg:grid-cols-3 gap-6
- Each result in Card component
- Improvement value LARGE (text-5xl font-bold text-primary)
- Metric name as CardTitle
- Before/After comparison in CardContent:
  - "Before: {before}" in muted color
  - "After: {after}" in primary color
- Use FadeIn with stagger delay (delay={index * 0.1}) for entrance animation

**TestimonialBlock.tsx:**
Display client testimonial quote:

Props: testimonial: { quote: string; author: string; role: string; company: string }

Structure:
- Section with bg-muted/50 rounded-lg p-8 my-12
- Large quote icon (Quote from lucide-react, text-primary/20, h-12 w-12)
- Quote text in text-xl italic
- Attribution: "{author}, {role} at {company}" in text-muted-foreground
- Wrap in FadeIn for entrance animation
  </action>
  <verify>npx tsc --noEmit src/components/case-study/MetricsDisplay.tsx src/components/case-study/TestimonialBlock.tsx</verify>
  <done>Both components compile, display metrics and testimonials with proper styling</done>
</task>

<task type="auto">
  <name>Task 3: Create WorkflowComparison, RelatedCaseStudies, and barrel export</name>
  <files>src/components/case-study/WorkflowComparison.tsx, src/components/case-study/RelatedCaseStudies.tsx, src/components/case-study/index.ts</files>
  <action>
**WorkflowComparison.tsx:**
Side-by-side before/after workflow images:

Props: beforeImage: string; afterImage: string

Structure:
- Grid md:grid-cols-2 gap-8 my-12
- Before column:
  - h3 "Before: Manual Process" in text-destructive
  - Relative container with aspect-square
  - Image with fill, object-contain
  - Border-2 border-destructive/20 rounded-lg
- After column:
  - h3 "After: Automated with RomAIx" in text-primary
  - Same image structure
  - Border-2 border-primary/20 rounded-lg

Use next/image Image component.

**RelatedCaseStudies.tsx:**
Related content recommendations:

Props: studies: Array<{ slug, title, client, industry, metric, metricValue }>

Structure:
- Section with mt-16 pt-12 border-t
- h2 "Related Case Studies" (text-2xl font-bold mb-8)
- Grid md:grid-cols-2 gap-6
- Render simplified CaseStudyCard for each (reuse component or inline simpler version)
- Only show if studies.length > 0

**index.ts:**
Barrel export all components:
export { CaseStudyCard } from './CaseStudyCard';
export { MetricsDisplay } from './MetricsDisplay';
export { TestimonialBlock } from './TestimonialBlock';
export { WorkflowComparison } from './WorkflowComparison';
export { RelatedCaseStudies } from './RelatedCaseStudies';
export { IndustryFilter } from './IndustryFilter';
  </action>
  <verify>npx tsc --noEmit src/components/case-study/index.ts</verify>
  <done>All components compile, barrel export works, imports from @/components/case-study succeed</done>
</task>

</tasks>

<verification>
1. All components compile: `npx tsc --noEmit`
2. Imports work: `import { CaseStudyCard, MetricsDisplay } from '@/components/case-study'`
3. CaseStudyCard links to correct route (/case-studies/[slug])
4. IndustryFilter uses URL params for navigation
5. Components use shadcn/ui Card where appropriate
6. FadeIn animations applied to key elements
</verification>

<success_criteria>
- 6 components created following blog component patterns
- CaseStudyCard matches homepage card styling (metric-first display)
- MetricsDisplay shows before/after with large improvement value
- TestimonialBlock renders quote with proper attribution
- WorkflowComparison shows side-by-side images
- RelatedCaseStudies renders recommendation grid
- IndustryFilter mirrors CategoryFilter behavior
- Barrel export enables clean imports
</success_criteria>

<output>
After completion, create `.planning/phases/07-case-studies-detail-pages/07-03-SUMMARY.md`
</output>
