---
phase: 07-case-studies-detail-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/schemas/case-study.ts
  - src/lib/case-studies.ts
autonomous: true

must_haves:
  truths:
    - "Case study frontmatter validates with Zod schema"
    - "getAllCaseStudies returns sorted array of case studies"
    - "getCaseStudyBySlug returns single case study or null"
    - "getRelatedCaseStudies returns tag-matched case studies"
  artifacts:
    - path: "src/lib/schemas/case-study.ts"
      provides: "Zod schema for case study metadata validation"
      exports: ["caseStudySchema", "CaseStudyMetadata"]
    - path: "src/lib/case-studies.ts"
      provides: "Case study data fetching utilities"
      exports: ["getAllCaseStudies", "getCaseStudyBySlug", "getRelatedCaseStudies", "getAllIndustries", "CaseStudy"]
  key_links:
    - from: "src/lib/case-studies.ts"
      to: "src/lib/schemas/case-study.ts"
      via: "import caseStudySchema"
      pattern: "import.*caseStudySchema.*from.*schemas/case-study"
    - from: "src/lib/case-studies.ts"
      to: "src/content/case-studies/"
      via: "fs.readFileSync"
      pattern: "caseStudiesDirectory"
---

<objective>
Create case study schema and data utilities mirroring Phase 6 blog implementation

Purpose: Establish type-safe foundation for case study pages with extended metadata (testimonials, results, workflows)
Output: Zod schema and utility functions matching getAllCaseStudies/getCaseStudyBySlug patterns
</objective>

<execution_context>
@/Users/yrs/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yrs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-case-studies-detail-pages/07-RESEARCH.md

# Reference implementation patterns
@src/lib/blog.ts
@src/lib/schemas/blog.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create case study Zod schema</name>
  <files>src/lib/schemas/case-study.ts</files>
  <action>
Create Zod schema for case study frontmatter validation with these fields:

Required fields:
- title: string (min 1)
- description: string (min 1)
- date: string (YYYY-MM-DD regex)
- client: string (min 1)
- industry: enum ['Tour Operator', 'Travel Agency', 'Boutique Hotels', 'Other']
- metric: string (primary metric name, e.g., "Response Time Reduced")
- metricValue: string (e.g., "85%")
- testimonial: nested object with quote, author, role, company (all required strings)
- results: array of objects with metric, before, after, improvement (all strings)
- slug: string (min 1)

Optional fields:
- beforeWorkflow: string (image path)
- afterWorkflow: string (image path)
- tags: array of strings (default [])

Export both caseStudySchema and CaseStudyMetadata type.

Follow exact pattern from src/lib/schemas/blog.ts - use z.object, z.string().min(), z.enum with explicit values.
  </action>
  <verify>TypeScript compiles: npx tsc --noEmit src/lib/schemas/case-study.ts</verify>
  <done>Schema file exists with all required fields, types export correctly</done>
</task>

<task type="auto">
  <name>Task 2: Create case study data utilities</name>
  <files>src/lib/case-studies.ts</files>
  <action>
Create utility file mirroring src/lib/blog.ts structure with:

1. Define caseStudiesDirectory pointing to src/content/case-studies
2. Define CaseStudy interface extending CaseStudyMetadata with slug and content

3. getAllCaseStudies(): CaseStudy[]
   - Check if directory exists (return [] if not)
   - Read all .mdx files from directory
   - Parse frontmatter with gray-matter
   - Validate with caseStudySchema.parse(data)
   - Return sorted by date descending

4. getCaseStudyBySlug(slug: string): CaseStudy | null
   - Build full path from slug
   - Return null if file doesn't exist
   - Parse and validate frontmatter
   - Return study with content

5. getRelatedCaseStudies(currentSlug: string, limit = 2): CaseStudy[]
   - Get all studies, find current
   - Calculate Jaccard similarity on tags + industry
   - Return top N by similarity score (excluding current)
   - Use Set intersection/union pattern from research

6. getAllIndustries(): string[]
   - Extract unique industries from all case studies
   - Return sorted array

Import gray-matter, fs, path as in blog.ts. Import schema from ./schemas/case-study.
  </action>
  <verify>TypeScript compiles: npx tsc --noEmit src/lib/case-studies.ts</verify>
  <done>All four functions implemented and type-safe, returns empty array when no content exists</done>
</task>

<task type="auto">
  <name>Task 3: Create content directory structure</name>
  <files>src/content/case-studies/.gitkeep, public/images/case-studies/.gitkeep</files>
  <action>
Create directory structure for case study content:

1. Create src/content/case-studies/ directory
2. Add .gitkeep file to preserve empty directory
3. Create public/images/case-studies/ directory
4. Add .gitkeep file to preserve empty directory

This ensures directories exist for Plan 02 content creation.
  </action>
  <verify>ls -la src/content/case-studies/ && ls -la public/images/case-studies/</verify>
  <done>Both directories exist with .gitkeep files</done>
</task>

</tasks>

<verification>
1. TypeScript compiles without errors: `npm run build` or `npx tsc --noEmit`
2. Schema exports are accessible: `import { caseStudySchema, CaseStudyMetadata } from '@/lib/schemas/case-study'`
3. Utility exports are accessible: `import { getAllCaseStudies, getCaseStudyBySlug } from '@/lib/case-studies'`
4. getAllCaseStudies() returns empty array (no content yet)
5. getCaseStudyBySlug('nonexistent') returns null
</verification>

<success_criteria>
- Case study schema validates extended metadata (testimonial, results, workflows)
- Utility functions mirror blog.ts patterns exactly
- Related case studies uses Jaccard similarity on tags
- Directory structure ready for content creation
- TypeScript types exported for use in components
</success_criteria>

<output>
After completion, create `.planning/phases/07-case-studies-detail-pages/07-01-SUMMARY.md`
</output>
