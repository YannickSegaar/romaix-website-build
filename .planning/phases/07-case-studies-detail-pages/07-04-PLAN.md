---
phase: 07-case-studies-detail-pages
plan: 04
type: execute
wave: 2
depends_on: ["07-01", "07-03"]
files_modified:
  - src/app/case-studies/page.tsx
  - src/app/case-studies/[slug]/page.tsx
  - src/components/sections/case-studies.tsx
autonomous: true

must_haves:
  truths:
    - "Visitor can view /case-studies listing with all case studies"
    - "Visitor can filter case studies by industry"
    - "Visitor can click case study to view /case-studies/[slug] detail page"
    - "Detail page shows hero metric, workflow images, results, testimonial"
    - "Detail page shows related case studies section"
    - "Homepage CaseStudies links work with new slug format"
  artifacts:
    - path: "src/app/case-studies/page.tsx"
      provides: "Case studies listing page"
      exports: ["default", "metadata"]
    - path: "src/app/case-studies/[slug]/page.tsx"
      provides: "Case study detail page"
      exports: ["default", "generateStaticParams", "generateMetadata"]
  key_links:
    - from: "src/app/case-studies/page.tsx"
      to: "src/lib/case-studies.ts"
      via: "getAllCaseStudies import"
      pattern: "import.*getAllCaseStudies.*from.*lib/case-studies"
    - from: "src/app/case-studies/[slug]/page.tsx"
      to: "src/lib/case-studies.ts"
      via: "getCaseStudyBySlug import"
      pattern: "import.*getCaseStudyBySlug.*from.*lib/case-studies"
    - from: "src/app/case-studies/[slug]/page.tsx"
      to: "next-mdx-remote/rsc"
      via: "MDXRemote for content"
      pattern: "MDXRemote.*source=.*content"
---

<objective>
Create case studies listing and detail pages with MDX rendering

Purpose: Complete case study pages enabling deep-dive exploration from homepage
Output: Listing page with filter, detail page with full content, updated homepage links
</objective>

<execution_context>
@/Users/yrs/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yrs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-case-studies-detail-pages/07-RESEARCH.md

# Reference patterns from blog
@src/app/blog/page.tsx
@src/app/blog/[slug]/page.tsx

# Components from Plan 03
# (Will be available: CaseStudyCard, MetricsDisplay, TestimonialBlock, etc.)

# Homepage section to update
@src/components/sections/case-studies.tsx
@src/data/case-studies.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create case studies listing page</name>
  <files>src/app/case-studies/page.tsx</files>
  <action>
Create listing page mirroring src/app/blog/page.tsx pattern:

1. Import getAllCaseStudies, getAllIndustries from @/lib/case-studies
2. Import CaseStudyCard, IndustryFilter from @/components/case-study
3. Import FadeIn, StaggerContainer, StaggerItem from @/components/motion

Page structure:
- Export metadata: title "Case Studies | RomAIx", description about real results

- Props: searchParams: Promise<{ industry?: string }>

- Await searchParams to get selectedIndustry (Next.js 15 pattern)

- Get all studies and industries from utilities

- Filter studies by selectedIndustry if provided

- Render:
  - FadeIn header with h1 "Case Studies" and subtitle
  - IndustryFilter component with industries and selectedIndustry
  - StaggerContainer grid (md:grid-cols-2 lg:grid-cols-3 gap-6)
  - Map filtered studies to CaseStudyCard wrapped in StaggerItem
  - Empty state message if no studies match filter

Follow exact pattern from blog/page.tsx for searchParams handling.
  </action>
  <verify>npm run build && curl -s http://localhost:3000/case-studies | grep -q "Case Studies"</verify>
  <done>Listing page renders with grid of case studies, filter works</done>
</task>

<task type="auto">
  <name>Task 2: Create case study detail page</name>
  <files>src/app/case-studies/[slug]/page.tsx</files>
  <action>
Create detail page mirroring src/app/blog/[slug]/page.tsx pattern:

1. Imports:
   - getCaseStudyBySlug, getAllCaseStudies, getRelatedCaseStudies from @/lib/case-studies
   - notFound from next/navigation
   - format from date-fns
   - FadeIn from @/components/motion
   - Link from next/link
   - ArrowLeft from lucide-react
   - MDXRemote from next-mdx-remote/rsc
   - MetricsDisplay, TestimonialBlock, WorkflowComparison, RelatedCaseStudies from @/components/case-study

2. Interface: CaseStudyPageProps { params: Promise<{ slug: string }> }

3. generateStaticParams(): Return all case study slugs

4. dynamicParams = false (404 for unknown slugs)

5. generateMetadata(): Get study by slug, return title and description

6. Default export async function:
   - Await params to get slug
   - Get study by slug
   - If null, call notFound()
   - Get related studies (limit 2)

   Render structure:
   - Back link to /case-studies
   - FadeIn article wrapper
   - Header: industry badge, h1 title, client name + date
   - Hero metric box (bg-primary/5 p-8): large metricValue, metric name
   - MDX content with prose classes
   - WorkflowComparison (if beforeWorkflow and afterWorkflow exist)
   - "The Results" section with MetricsDisplay
   - TestimonialBlock
   - RelatedCaseStudies (if any)

CRITICAL: Await params before accessing slug (Next.js 15 requirement).
  </action>
  <verify>npm run build && test -f .next/server/app/case-studies/adventure-tours.html</verify>
  <done>Detail page renders with all sections, MDX content displays, static generation works</done>
</task>

<task type="auto">
  <name>Task 3: Update homepage CaseStudies component slugs</name>
  <files>src/components/sections/case-studies.tsx, src/data/case-studies.ts</files>
  <action>
Update slugs to match new MDX filenames:

**src/data/case-studies.ts:**
Change slugs to match MDX filenames:
- 'adventure-tours-response-automation' -> 'adventure-tours'
- 'coastal-retreats-booking-automation' -> 'coastal-retreats'
- 'euro-expeditions-lead-nurturing' -> 'euro-expeditions'

This ensures homepage "Read case study" links work with new detail pages.

**src/components/sections/case-studies.tsx:**
No changes needed if it already uses study.slug for Link href.

Verify the Link component uses:
href={`/case-studies/${study.slug}`}

If homepage CaseStudies component works correctly, no code changes needed.
  </action>
  <verify>npm run dev && curl -s http://localhost:3000 | grep -o '/case-studies/[^"]*' | head -3</verify>
  <done>Homepage links point to correct slugs, clicking "Read case study" navigates to detail page</done>
</task>

</tasks>

<verification>
1. /case-studies listing page renders with 3 case studies
2. Industry filter changes URL and filters list
3. Clicking case study card navigates to /case-studies/[slug]
4. Detail page displays all sections (metric hero, MDX, workflow, results, testimonial, related)
5. MDX content renders with prose styling
6. Homepage CaseStudies links work (click "Read case study" -> detail page)
7. Build completes without errors: npm run build
8. Static pages generated for all slugs
</verification>

<success_criteria>
- Case studies listing page at /case-studies with industry filter
- Detail pages at /case-studies/[slug] with full content
- MDX rendering via next-mdx-remote/rsc (Turbopack compatible)
- Workflow before/after images display
- Quantified results with MetricsDisplay
- Client testimonial with TestimonialBlock
- Related case studies recommendations
- Homepage links work with new slug format
- generateStaticParams enables static generation
</success_criteria>

<output>
After completion, create `.planning/phases/07-case-studies-detail-pages/07-04-SUMMARY.md`
</output>
