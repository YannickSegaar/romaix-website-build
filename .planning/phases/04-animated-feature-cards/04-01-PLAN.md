---
phase: 04-animated-feature-cards
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/content.ts
  - src/data/solutions.ts
  - src/components/motion/hover-card.tsx
  - src/components/motion/index.ts
autonomous: true

must_haves:
  truths:
    - "Feature card type defines title, description, icon, and optional highlight flag"
    - "HoverCard wrapper animates scale and shadow on hover with GPU-accelerated transforms"
    - "HoverCard provides whileTap fallback for mobile touch devices"
  artifacts:
    - path: "src/types/content.ts"
      provides: "FeatureCard and Integration types"
      contains: "interface FeatureCard"
    - path: "src/data/solutions.ts"
      provides: "Static feature card data for Solutions section"
      exports: ["FEATURES", "INTEGRATIONS"]
    - path: "src/components/motion/hover-card.tsx"
      provides: "Reusable whileHover animation wrapper"
      exports: ["HoverCard"]
    - path: "src/components/motion/index.ts"
      provides: "Barrel export including HoverCard"
      contains: "export { HoverCard }"
  key_links:
    - from: "src/components/motion/hover-card.tsx"
      to: "framer-motion"
      via: "whileHover and whileTap props"
      pattern: "whileHover.*scale"
---

<objective>
Create feature card types, content data, and HoverCard animation wrapper for Solutions section

Purpose: Establish data infrastructure and reusable hover animation component that enables smooth card interactions
Output: Types, data files, and HoverCard component ready for Solutions section assembly
</objective>

<execution_context>
@/Users/yrs/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yrs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-animated-feature-cards/04-RESEARCH.md
@.planning/phases/01-foundation-setup/01-03-SUMMARY.md
@src/types/content.ts
@src/components/motion/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add FeatureCard and Integration types to content.ts</name>
  <files>src/types/content.ts</files>
  <action>
Add two new interfaces to the existing content.ts file:

```typescript
// Feature card for Solutions section
export interface FeatureCard {
  id: string
  title: string
  description: string
  icon: string // Lucide icon name (e.g., "Bot", "Workflow", "Plug")
  isHero?: boolean // If true, card gets enhanced hover effects
}

// Integration platform for logo display
export interface Integration {
  name: string
  logo: string // Path to SVG icon (e.g., "/icons/whatsapp.svg")
}
```

Keep existing types (CaseStudy, FAQItem, Step) unchanged. Add new types at the end of the file.
  </action>
  <verify>Run `npx tsc --noEmit` to verify types compile without errors</verify>
  <done>FeatureCard and Integration interfaces exported from src/types/content.ts</done>
</task>

<task type="auto">
  <name>Task 2: Create solutions.ts data file with features and integrations</name>
  <files>src/data/solutions.ts</files>
  <action>
Create new data file with feature cards and integration logos:

```typescript
import type { FeatureCard, Integration } from '@/types/content'

// Feature cards for Solutions section
// 3 hero cards (with enhanced hover) + 3 supporting cards
export const FEATURES: FeatureCard[] = [
  // Hero cards (isHero: true)
  {
    id: 'ai-agents',
    title: 'AI Booking Agents',
    description: 'Intelligent agents that handle booking inquiries 24/7, answering questions and guiding customers through the reservation process.',
    icon: 'Bot',
    isHero: true,
  },
  {
    id: 'workflow-automation',
    title: 'Workflow Automation',
    description: 'Automate repetitive tasks like booking confirmations, follow-ups, and payment reminders to save hours every week.',
    icon: 'Workflow',
    isHero: true,
  },
  {
    id: 'integrations',
    title: 'Platform Integrations',
    description: 'Connect your existing tools seamlessly. We integrate with booking systems, CRMs, and communication platforms.',
    icon: 'Plug',
    isHero: true,
  },
  // Supporting cards
  {
    id: 'multi-channel',
    title: 'Multi-Channel Support',
    description: 'Manage conversations across WhatsApp, Instagram, Facebook, and email from a single dashboard.',
    icon: 'MessageSquare',
  },
  {
    id: 'analytics',
    title: 'Smart Analytics',
    description: 'Track response times, conversion rates, and customer satisfaction with real-time dashboards.',
    icon: 'BarChart3',
  },
  {
    id: 'custom-flows',
    title: 'Custom Conversation Flows',
    description: 'Design personalized customer journeys that match your brand voice and business processes.',
    icon: 'GitBranch',
  },
]

// Integration platform logos
// Note: Icons will be placeholder divs initially, replaced with real SVGs later
export const INTEGRATIONS: Integration[] = [
  { name: 'WhatsApp', logo: '/icons/whatsapp.svg' },
  { name: 'Instagram', logo: '/icons/instagram.svg' },
  { name: 'Facebook', logo: '/icons/facebook.svg' },
  { name: 'Messenger', logo: '/icons/messenger.svg' },
  { name: 'Email', logo: '/icons/email.svg' },
  { name: 'Zapier', logo: '/icons/zapier.svg' },
  { name: 'HubSpot', logo: '/icons/hubspot.svg' },
  { name: 'Salesforce', logo: '/icons/salesforce.svg' },
]
```

The descriptions focus on travel industry use cases per PROJECT.md.
  </action>
  <verify>Run `npx tsc --noEmit` to verify imports resolve and types match</verify>
  <done>FEATURES and INTEGRATIONS arrays exported with travel-industry-focused content</done>
</task>

<task type="auto">
  <name>Task 3: Create HoverCard animation wrapper component</name>
  <files>src/components/motion/hover-card.tsx, src/components/motion/index.ts</files>
  <action>
Create HoverCard component following the existing animation wrapper pattern from Phase 1.

src/components/motion/hover-card.tsx:
```typescript
'use client'

import { motion, HTMLMotionProps } from 'framer-motion'

interface HoverCardProps extends HTMLMotionProps<'div'> {
  children: React.ReactNode
  className?: string
  /** Enhanced hover effect for hero cards (larger scale, more shadow) */
  enhanced?: boolean
}

export function HoverCard({
  children,
  className,
  enhanced = false,
  ...props
}: HoverCardProps) {
  // Scale values: enhanced (hero) cards scale more
  const hoverScale = enhanced ? 1.05 : 1.02
  const tapScale = 0.98

  return (
    <motion.div
      whileHover={{
        scale: hoverScale,
        transition: { duration: 0.3, ease: 'easeOut' },
      }}
      whileTap={{ scale: tapScale }}
      className={className}
      {...props}
    >
      {children}
    </motion.div>
  )
}
```

Update src/components/motion/index.ts to add the export:
```typescript
export { HoverCard } from './hover-card'
```

Key implementation notes:
- Uses 'use client' directive per Phase 1 pattern
- whileHover for desktop, whileTap for mobile touch fallback
- Only animates scale (GPU-accelerated transform) per research recommendations
- enhanced prop for hero cards (isHero: true) to get larger scale effect
- Accepts all HTMLMotionProps for flexibility (like FadeIn and SlideIn)
  </action>
  <verify>
Run `npm run build` to verify component compiles. Create a quick test in page.tsx if needed to verify hover effect works (can remove after testing).
  </verify>
  <done>HoverCard component exported from @/components/motion with scale hover animation and mobile tap fallback</done>
</task>

</tasks>

<verification>
After completing all tasks:
1. `npx tsc --noEmit` passes with no type errors
2. `npm run build` succeeds
3. src/types/content.ts contains FeatureCard and Integration interfaces
4. src/data/solutions.ts exports FEATURES (6 items) and INTEGRATIONS (8 items)
5. src/components/motion/index.ts exports HoverCard alongside existing FadeIn, SlideIn, StaggerContainer, StaggerItem
</verification>

<success_criteria>
- FeatureCard type has id, title, description, icon, and optional isHero fields
- Integration type has name and logo fields
- FEATURES array has 3 hero cards and 3 supporting cards with travel-industry content
- INTEGRATIONS array has 8 platform logos (WhatsApp, Instagram, Facebook, Messenger, Email, Zapier, HubSpot, Salesforce)
- HoverCard component provides whileHover scale animation with enhanced option for hero cards
- HoverCard includes whileTap fallback for mobile devices
- All components compile without TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/04-animated-feature-cards/04-01-SUMMARY.md`
</output>
