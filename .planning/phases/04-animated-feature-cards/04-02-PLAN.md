---
phase: 04-animated-feature-cards
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/sections/solutions.tsx
  - src/components/sections/index.ts
  - public/icons/whatsapp.svg
  - public/icons/instagram.svg
  - public/icons/facebook.svg
  - public/icons/messenger.svg
  - public/icons/email.svg
  - public/icons/zapier.svg
  - public/icons/hubspot.svg
  - public/icons/salesforce.svg
autonomous: true

must_haves:
  truths:
    - "Solutions section displays feature cards in responsive grid layout"
    - "Integration logos appear in grid with grayscale-to-color hover effect"
    - "Section uses StaggerContainer for scroll-triggered reveal"
  artifacts:
    - path: "src/components/sections/solutions.tsx"
      provides: "Solutions section with FeatureCard and IntegrationLogos components"
      exports: ["Solutions"]
    - path: "src/components/sections/index.ts"
      provides: "Barrel export including Solutions"
      contains: "export { Solutions }"
    - path: "public/icons/whatsapp.svg"
      provides: "WhatsApp integration logo"
  key_links:
    - from: "src/components/sections/solutions.tsx"
      to: "@/components/motion"
      via: "StaggerContainer, StaggerItem imports"
      pattern: "import.*StaggerContainer"
    - from: "src/components/sections/solutions.tsx"
      to: "lucide-react"
      via: "Dynamic icon rendering"
      pattern: "import.*from 'lucide-react'"
---

<objective>
Create Solutions section with feature cards grid and integration logos display

Purpose: Build the visual structure for the animated feature cards section with staggered scroll reveal
Output: Solutions component ready for homepage integration, with placeholder integration logos
</objective>

<execution_context>
@/Users/yrs/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yrs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-animated-feature-cards/04-RESEARCH.md
@.planning/phases/01-foundation-setup/01-03-SUMMARY.md
@src/components/sections/index.ts
@src/components/sections/hero.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create placeholder SVG icons for integrations</name>
  <files>public/icons/*.svg</files>
  <action>
Create the public/icons directory if it doesn't exist and add simple placeholder SVG icons for each integration platform.

Each SVG should be a simple 24x24 placeholder with the platform's initial letter or symbol. These will be replaced with official brand icons later.

Example placeholder pattern (adjust color and letter for each):
```xml
<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
  <circle cx="12" cy="12" r="10"/>
  <text x="12" y="16" text-anchor="middle" font-size="10" fill="currentColor" stroke="none">W</text>
</svg>
```

Create these files:
- public/icons/whatsapp.svg (W)
- public/icons/instagram.svg (I)
- public/icons/facebook.svg (F)
- public/icons/messenger.svg (M)
- public/icons/email.svg (E)
- public/icons/zapier.svg (Z)
- public/icons/hubspot.svg (H)
- public/icons/salesforce.svg (S)

Note: These are simple placeholders. The user will replace them with official brand icons before launch.
  </action>
  <verify>Verify files exist with `ls public/icons/` showing 8 SVG files</verify>
  <done>8 placeholder SVG icons created in public/icons/ directory</done>
</task>

<task type="auto">
  <name>Task 2: Create Solutions section component</name>
  <files>src/components/sections/solutions.tsx</files>
  <action>
Create Solutions section following existing section patterns (Hero, CaseStudies) as Server Component with animation wrappers.

```typescript
import { StaggerContainer, StaggerItem } from '@/components/motion'
import { FEATURES, INTEGRATIONS } from '@/data/solutions'
import {
  Bot,
  Workflow,
  Plug,
  MessageSquare,
  BarChart3,
  GitBranch,
} from 'lucide-react'
import Image from 'next/image'

// Map icon names to Lucide components
const iconMap: Record<string, React.ElementType> = {
  Bot,
  Workflow,
  Plug,
  MessageSquare,
  BarChart3,
  GitBranch,
}

export function Solutions() {
  return (
    <section className="py-24">
      <div className="container mx-auto px-4">
        {/* Section Header */}
        <div className="mb-16 text-center">
          <h2 className="mb-4 text-4xl font-bold tracking-tight md:text-5xl">
            Solutions That Transform
          </h2>
          <p className="mx-auto max-w-2xl text-lg text-muted-foreground">
            AI-powered automation tools designed specifically for tour operators
            and travel businesses
          </p>
        </div>

        {/* Feature Cards Grid */}
        <StaggerContainer
          staggerDelay={0.15}
          delayChildren={0.2}
          className="mb-20 grid gap-6 md:grid-cols-2 lg:grid-cols-3"
        >
          {FEATURES.map((feature) => {
            const IconComponent = iconMap[feature.icon] || Bot
            return (
              <StaggerItem key={feature.id}>
                <div
                  className={`group rounded-xl border-2 bg-card p-6 transition-all duration-300 hover:shadow-xl hover:scale-[1.02] ${
                    feature.isHero
                      ? 'border-primary/30 hover:border-primary/50 hover:scale-[1.05]'
                      : 'border-border hover:border-primary/20'
                  }`}
                >
                  <div
                    className={`mb-4 inline-flex rounded-lg p-3 ${
                      feature.isHero
                        ? 'bg-primary/10 text-primary'
                        : 'bg-muted text-muted-foreground group-hover:bg-primary/10 group-hover:text-primary'
                    } transition-colors`}
                  >
                    <IconComponent className="h-6 w-6" />
                  </div>
                  <h3 className="mb-2 text-xl font-semibold">{feature.title}</h3>
                  <p className="text-muted-foreground">{feature.description}</p>
                </div>
              </StaggerItem>
            )
          })}
        </StaggerContainer>

        {/* Integrations Section */}
        <div className="text-center">
          <p className="mb-8 text-sm font-medium uppercase tracking-wider text-muted-foreground">
            Integrates With Your Favorite Tools
          </p>
          <div className="grid grid-cols-4 gap-4 md:grid-cols-8">
            {INTEGRATIONS.map((integration) => (
              <div
                key={integration.name}
                className="flex aspect-square items-center justify-center rounded-lg border bg-muted/30 p-4 grayscale transition-all hover:bg-muted/50 hover:grayscale-0"
                title={integration.name}
              >
                <Image
                  src={integration.logo}
                  alt={integration.name}
                  width={32}
                  height={32}
                  className="h-8 w-8 object-contain"
                />
              </div>
            ))}
          </div>
        </div>
      </div>
    </section>
  )
}
```

Key implementation notes:
- Server Component (no 'use client') - animations via StaggerContainer wrapper
- Hero cards (isHero: true) get enhanced styling: primary border, larger scale on hover
- Uses CSS hover (hover:scale-[1.02]) instead of Framer Motion whileHover for simplicity
- Integration logos use grayscale-to-color transition per research pattern
- Responsive grid: 1 col mobile, 2 cols tablet, 3 cols desktop for cards
- Integration grid: 4 cols mobile, 8 cols desktop
  </action>
  <verify>Run `npm run build` to verify component compiles without errors</verify>
  <done>Solutions section component created with feature cards grid and integration logos</done>
</task>

<task type="auto">
  <name>Task 3: Add Solutions to sections barrel export</name>
  <files>src/components/sections/index.ts</files>
  <action>
Add Solutions export to the existing barrel file:

```typescript
export { Solutions } from './solutions'
```

Add this line in alphabetical order with the other exports, or at the end of the file.
  </action>
  <verify>Run `npx tsc --noEmit` to verify export resolves correctly</verify>
  <done>Solutions exported from @/components/sections barrel</done>
</task>

</tasks>

<verification>
After completing all tasks:
1. `ls public/icons/` shows 8 SVG files
2. `npm run build` succeeds
3. Solutions component is exported from @/components/sections
4. Component uses StaggerContainer for scroll-triggered reveal
5. Integration logos have grayscale-to-color hover effect
</verification>

<success_criteria>
- 8 placeholder SVG icons exist in public/icons/
- Solutions section displays 6 feature cards in responsive grid (3 hero + 3 supporting)
- Hero cards have enhanced visual treatment (primary border tint, larger hover scale)
- Integration logos display in 4-col (mobile) / 8-col (desktop) grid
- Integration logos have grayscale default, full color on hover
- StaggerContainer provides scroll-triggered staggered reveal
- Component follows existing section patterns (Server Component with animation wrappers)
</success_criteria>

<output>
After completion, create `.planning/phases/04-animated-feature-cards/04-02-SUMMARY.md`
</output>
