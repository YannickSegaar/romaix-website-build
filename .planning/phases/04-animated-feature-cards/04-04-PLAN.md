---
phase: 04-animated-feature-cards
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/motion/hover-card.tsx
  - src/components/motion/index.ts
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "No orphaned components exist in the motion components directory"
    - "Motion barrel export only exports components that are actually used"
  artifacts:
    - path: "src/components/motion/index.ts"
      provides: "Motion component exports"
      not_contains: "HoverCard"
  key_links: []
---

<objective>
Remove orphaned HoverCard component to close verification gap.

Purpose: The VERIFICATION.md found that HoverCard was created (04-01) but the Solutions component (04-02) uses CSS hover effects instead. Since CSS transforms are equally GPU-accelerated and the implementation is simpler, we remove the unused component rather than refactoring Solutions.

Output: Clean motion components directory with no dead code.
</objective>

<execution_context>
@/Users/yrs/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yrs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-animated-feature-cards/04-VERIFICATION.md

# Current state of files to modify
@src/components/motion/index.ts
@src/components/motion/hover-card.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove HoverCard component and update barrel export</name>
  <files>
    src/components/motion/hover-card.tsx
    src/components/motion/index.ts
  </files>
  <action>
1. Delete src/components/motion/hover-card.tsx entirely

2. Update src/components/motion/index.ts to remove the HoverCard export line:
   - Remove: `export { HoverCard } from './hover-card'`
   - Keep all other exports (FadeIn, SlideIn, StaggerContainer, StaggerItem)

The resulting index.ts should be:
```ts
export { FadeIn } from './fade-in'
export { SlideIn } from './slide-in'
export { StaggerContainer, StaggerItem } from './stagger-container'
```
  </action>
  <verify>
1. File deleted: `ls src/components/motion/hover-card.tsx` should fail (file not found)
2. Grep check: `grep -r "HoverCard" src/` returns no results
3. Build passes: `npm run build` completes without errors
  </verify>
  <done>
- hover-card.tsx file no longer exists
- Motion barrel export has no HoverCard reference
- Build succeeds with no missing import errors
  </done>
</task>

</tasks>

<verification>
1. `ls src/components/motion/` shows only: fade-in.tsx, slide-in.tsx, stagger-container.tsx, index.ts
2. `grep -r "HoverCard" src/` returns empty (no references anywhere)
3. `npm run build` succeeds
4. Dev server runs without errors: `npm run dev`
</verification>

<success_criteria>
- HoverCard component removed from codebase
- No references to HoverCard anywhere in src/
- Build and dev server work correctly
- Verification gap closed: no orphaned components
</success_criteria>

<output>
After completion, create `.planning/phases/04-animated-feature-cards/04-04-SUMMARY.md`
</output>
