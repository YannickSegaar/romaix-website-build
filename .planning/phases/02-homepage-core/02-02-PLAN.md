---
phase: 02-homepage-core
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - src/components/sections/hero.tsx
  - src/components/sections/social-proof.tsx
  - src/components/sections/index.ts
autonomous: true

must_haves:
  truths:
    - "Hero section displays value proposition headline under 8 words"
    - "Hero section has two CTAs: Book a Demo (primary) and Get Free Assessment (secondary)"
    - "CTAs link to contact page with type parameter (?type=demo, ?type=assessment)"
    - "Social proof section displays client logos in infinite scrolling marquee"
    - "Logo marquee pauses on hover for readability"
    - "Logos display in grayscale and transition to color on hover"
  artifacts:
    - path: "src/components/sections/hero.tsx"
      provides: "Hero section with value proposition and CTAs"
      contains: "Book a Demo"
    - path: "src/components/sections/social-proof.tsx"
      provides: "Logo marquee with CSS animation"
      contains: "animate-marquee"
    - path: "src/components/sections/index.ts"
      provides: "Barrel export for section components"
      exports: ["Hero", "SocialProof"]
  key_links:
    - from: "src/components/sections/hero.tsx"
      to: "/contact"
      via: "Link component href"
      pattern: "href.*contact.*type="
    - from: "src/components/sections/social-proof.tsx"
      to: "src/app/globals.css"
      via: "animate-marquee class"
      pattern: "animate-marquee"
---

<objective>
Create the Hero and Social Proof sections that form the top of the homepage, establishing value proposition and credibility immediately.

Purpose: These are the first things visitors see - Hero must communicate value in 5 seconds, Social Proof must establish trust immediately after.
Output: Hero section component with CTAs, Social Proof logo marquee component, barrel export file.
</objective>

<execution_context>
@/Users/yrs/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yrs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-homepage-core/02-RESEARCH.md
@src/components/motion/index.ts
@src/components/ui/button.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Hero section component</name>
  <files>src/components/sections/hero.tsx</files>
  <action>
Create `src/components/sections/` directory if it doesn't exist.

Create `src/components/sections/hero.tsx` as a Server Component (no 'use client'):

```typescript
import Link from 'next/link'
import { Button } from '@/components/ui/button'
import { FadeIn } from '@/components/motion'

export function Hero() {
  return (
    <section className="container px-4 md:px-6 py-16 md:py-24 lg:py-32">
      <FadeIn className="text-center max-w-4xl mx-auto">
        <h1 className="text-4xl md:text-5xl lg:text-6xl font-bold tracking-tight">
          AI Automation for{' '}
          <span className="text-primary">Travel Businesses</span>
        </h1>
        <p className="mt-6 text-lg md:text-xl text-muted-foreground max-w-2xl mx-auto">
          Save 40+ hours per week. Automate customer service, bookings, and workflows
          with AI agents built for tour operators, travel agencies, and boutique hotels.
        </p>
        <div className="mt-8 flex flex-col sm:flex-row gap-4 justify-center">
          <Button size="lg" asChild>
            <Link href="/contact?type=demo">Book a Demo</Link>
          </Button>
          <Button size="lg" variant="outline" asChild>
            <Link href="/contact?type=assessment">Get Free Assessment</Link>
          </Button>
        </div>
      </FadeIn>
    </section>
  )
}
```

Key design decisions:
- Headline under 8 words: "AI Automation for Travel Businesses" (5 words)
- Primary brand color on "Travel Businesses" for visual emphasis
- Subheadline leads with benefit (40+ hours saved) before features
- Two CTAs using Button asChild pattern with Link for proper navigation
- Responsive padding (py-16 mobile, py-24 tablet, py-32 desktop)
- FadeIn wrapper for scroll-triggered animation
- Server Component (no 'use client') - FadeIn handles client-side animation internally
  </action>
  <verify>
1. File exists: `ls src/components/sections/hero.tsx`
2. TypeScript compiles: `npx tsc --noEmit`
3. Contains required elements: `grep -E "(Book a Demo|Get Free Assessment|type=demo|type=assessment)" src/components/sections/hero.tsx`
  </verify>
  <done>
- Hero component at src/components/sections/hero.tsx
- Headline is 5 words (under 8 word limit)
- Two CTAs with proper Link hrefs including type parameters
- Uses FadeIn for animation
- Server Component (no 'use client' directive)
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Social Proof logo marquee</name>
  <files>src/components/sections/social-proof.tsx</files>
  <action>
Create `src/components/sections/social-proof.tsx` as a Server Component:

```typescript
import Image from 'next/image'

// Placeholder logos - replace with actual client logos
// Using descriptive names that match what real travel industry clients would be
const logos = [
  { name: 'TravelTech Partners', src: '/logos/placeholder-1.svg' },
  { name: 'Adventure Tours Global', src: '/logos/placeholder-2.svg' },
  { name: 'Coastal Retreats', src: '/logos/placeholder-3.svg' },
  { name: 'Euro Expeditions', src: '/logos/placeholder-4.svg' },
  { name: 'Island Escapes', src: '/logos/placeholder-5.svg' },
  { name: 'Mountain Adventures', src: '/logos/placeholder-6.svg' },
]

export function SocialProof() {
  return (
    <section className="py-12 overflow-hidden bg-muted/30">
      <div className="text-center mb-8">
        <p className="text-sm text-muted-foreground font-medium uppercase tracking-wider">
          Trusted by leading travel businesses
        </p>
      </div>

      <div className="relative">
        {/* Gradient masks for smooth edges */}
        <div className="absolute left-0 top-0 bottom-0 w-16 md:w-24 bg-gradient-to-r from-muted/30 to-transparent z-10 pointer-events-none" />
        <div className="absolute right-0 top-0 bottom-0 w-16 md:w-24 bg-gradient-to-l from-muted/30 to-transparent z-10 pointer-events-none" />

        {/* Marquee container - duplicated logos for seamless loop */}
        <div className="flex animate-marquee">
          {[...logos, ...logos].map((logo, index) => (
            <div
              key={`${logo.name}-${index}`}
              className="flex-shrink-0 mx-6 md:mx-10"
            >
              {/* Placeholder: colored div with company initial */}
              {/* Replace with actual Image component when logos are available */}
              <div
                className="h-8 md:h-10 w-24 md:w-32 bg-muted-foreground/20 rounded flex items-center justify-center text-xs text-muted-foreground font-medium grayscale opacity-60 hover:grayscale-0 hover:opacity-100 transition-all duration-300"
                title={logo.name}
              >
                {logo.name.split(' ').map(w => w[0]).join('')}
              </div>
            </div>
          ))}
        </div>
      </div>

      {/* Alternative: Uncomment when real logos are available
      <div className="flex animate-marquee">
        {[...logos, ...logos].map((logo, index) => (
          <div key={`${logo.name}-${index}`} className="flex-shrink-0 mx-6 md:mx-10">
            <Image
              src={logo.src}
              alt={logo.name}
              width={128}
              height={40}
              className="h-8 md:h-10 w-auto grayscale opacity-60 hover:grayscale-0 hover:opacity-100 transition-all duration-300"
            />
          </div>
        ))}
      </div>
      */}
    </section>
  )
}
```

Key design decisions:
- CSS-only animation using `animate-marquee` class from globals.css (better performance than JS)
- Logos duplicated for seamless infinite loop (array spread: [...logos, ...logos])
- Grayscale by default, color on hover (industry standard for logo bars)
- Gradient masks on edges for smooth fade effect
- Pause on hover via CSS (defined in globals.css)
- Placeholder divs with initials until real logos are available
- Background uses muted/30 for subtle section separation
- Server Component - no interactivity needed beyond CSS hover

Note: Real logos should be SVGs placed in /public/logos/ directory. The placeholder implementation shows layout and animation behavior.
  </action>
  <verify>
1. File exists: `ls src/components/sections/social-proof.tsx`
2. TypeScript compiles: `npx tsc --noEmit`
3. Uses marquee class: `grep "animate-marquee" src/components/sections/social-proof.tsx`
4. Has gradient masks: `grep "gradient" src/components/sections/social-proof.tsx`
  </verify>
  <done>
- SocialProof component at src/components/sections/social-proof.tsx
- Uses CSS-only animate-marquee animation
- Logos duplicated for seamless infinite loop
- Grayscale with color-on-hover effect
- Gradient edge masks for smooth appearance
- Placeholder content ready for real logo replacement
  </done>
</task>

<task type="auto">
  <name>Task 3: Create sections barrel export</name>
  <files>src/components/sections/index.ts</files>
  <action>
Create `src/components/sections/index.ts` barrel export:

```typescript
export { Hero } from './hero'
export { SocialProof } from './social-proof'
```

This enables clean imports: `import { Hero, SocialProof } from '@/components/sections'`

Note: This file will be extended in subsequent plans as more sections are added.
  </action>
  <verify>
1. File exists: `ls src/components/sections/index.ts`
2. Exports work: `npx tsx -e "import { Hero, SocialProof } from './src/components/sections'; console.log('Imports OK')"`
  </verify>
  <done>
- Barrel export at src/components/sections/index.ts
- Exports Hero and SocialProof components
- Clean import path available: @/components/sections
  </done>
</task>

</tasks>

<verification>
1. `npm run build` completes without errors
2. Hero component renders with two CTAs linking to /contact with type parameters
3. SocialProof component displays scrolling logo marquee
4. Components are importable from @/components/sections
</verification>

<success_criteria>
- Hero section has clear value proposition under 8 words
- Hero has two CTAs with correct link destinations
- Social Proof displays infinite scrolling logo marquee
- Marquee pauses on hover
- Logos have grayscale-to-color hover effect
- Both components are Server Components (no 'use client')
- Barrel export enables clean imports
</success_criteria>

<output>
After completion, create `.planning/phases/02-homepage-core/02-02-SUMMARY.md`
</output>
