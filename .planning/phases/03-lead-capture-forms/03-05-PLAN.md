---
phase: 03-lead-capture-forms
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/contact/page.tsx
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Visitor clicking Hero 'Book a Demo' CTA sees DemoForm on /contact page"
    - "Visitor clicking Hero 'Get Free Assessment' CTA sees AssessmentForm on /contact page"
    - "Visitor navigating to /contact without query params sees ContactForm"
  artifacts:
    - path: "src/app/contact/page.tsx"
      provides: "Contact page route with conditional form rendering"
      min_lines: 40
  key_links:
    - from: "src/app/contact/page.tsx"
      to: "src/components/forms/*"
      via: "imports ContactForm, AssessmentForm, DemoForm"
      pattern: "import.*from.*@/components/forms"
    - from: "src/components/sections/hero.tsx"
      to: "src/app/contact/page.tsx"
      via: "Link href /contact?type=demo and /contact?type=assessment"
      pattern: "href.*contact.*type"
---

<objective>
Create the /contact page route that renders different forms based on URL query parameter.

Purpose: Fix broken Hero and FreeAssessment CTAs that currently 404. This completes the lead capture user flow.

Output: Working /contact page at src/app/contact/page.tsx with conditional form rendering.
</objective>

<execution_context>
@/Users/yrs/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yrs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-lead-capture-forms/03-VERIFICATION.md

Existing form components ready to use:
@src/components/forms/index.ts (barrel exports ContactForm, AssessmentForm, DemoForm)

Contact section design reference (for consistent styling):
@src/components/sections/contact.tsx

Hero shows expected URL pattern:
@src/components/sections/hero.tsx (lines 18-24: links to /contact?type=demo and /contact?type=assessment)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create /contact page with conditional form rendering</name>
  <files>src/app/contact/page.tsx</files>
  <action>
Create src/app/contact/page.tsx as a Server Component that:

1. Accept searchParams prop (Next.js 15 App Router pattern for server components):
   ```typescript
   export default async function ContactPage({
     searchParams,
   }: {
     searchParams: Promise<{ type?: string }>
   })
   ```
   Note: In Next.js 15, searchParams is a Promise that must be awaited.

2. Determine form type from query parameter:
   - Await searchParams, then extract `type` value
   - Valid types: 'demo', 'assessment', or undefined/other (default to contact)

3. Render page structure matching Contact section styling:
   - Use container with same padding as Contact section (py-16 md:py-24)
   - Centered layout with max-w-2xl for form area
   - Dynamic heading based on form type:
     - type=demo: "Book a Demo"
     - type=assessment: "Get Your Free Assessment"
     - default: "Get In Touch"
   - Subheading with form-appropriate copy

4. Conditionally render the appropriate form component:
   - Import { ContactForm, AssessmentForm, DemoForm } from '@/components/forms'
   - Render DemoForm when type === 'demo'
   - Render AssessmentForm when type === 'assessment'
   - Render ContactForm as default

5. Wrap content with FadeIn animation wrapper for consistency:
   - Import { FadeIn } from '@/components/motion'

6. Add metadata export for SEO (simple static metadata is fine):
   ```typescript
   export const metadata = {
     title: 'Contact Us | RomAIx',
     description: 'Get in touch with RomAIx for AI automation solutions for your travel business.',
   }
   ```

Design reference: Match the form container styling from Contact section (bg-muted/30 p-6 md:p-8 rounded-lg)
  </action>
  <verify>
Run development server and test:
1. `npm run dev`
2. Navigate to http://localhost:3000/contact - should show ContactForm
3. Navigate to http://localhost:3000/contact?type=demo - should show DemoForm
4. Navigate to http://localhost:3000/contact?type=assessment - should show AssessmentForm
5. Click Hero CTAs - should navigate to correct form
6. TypeScript compilation passes: `npx tsc --noEmit`
  </verify>
  <done>
/contact route renders appropriate form based on query parameter:
- No params or invalid type → ContactForm with "Get In Touch" heading
- ?type=demo → DemoForm with "Book a Demo" heading
- ?type=assessment → AssessmentForm with "Get Your Free Assessment" heading
All Hero and FreeAssessment CTAs now work (no 404).
  </done>
</task>

</tasks>

<verification>
After completing the task:

1. Route accessibility:
   - `curl -s http://localhost:3000/contact | grep -q "Get In Touch"` returns match
   - `curl -s "http://localhost:3000/contact?type=demo" | grep -q "Book a Demo"` returns match
   - `curl -s "http://localhost:3000/contact?type=assessment" | grep -q "Assessment"` returns match

2. CTA navigation (manual):
   - Hero "Book a Demo" button navigates to /contact?type=demo and shows DemoForm
   - Hero "Get Free Assessment" button navigates to /contact?type=assessment and shows AssessmentForm
   - FreeAssessment section CTAs work identically

3. Build verification:
   - `npm run build` completes without errors

4. No regressions:
   - Homepage still renders correctly with all sections
   - Contact section on homepage still shows ContactForm
</verification>

<success_criteria>
- src/app/contact/page.tsx exists with 40+ lines
- Page renders ContactForm for /contact with no query params
- Page renders DemoForm for /contact?type=demo
- Page renders AssessmentForm for /contact?type=assessment
- Hero and FreeAssessment CTAs no longer 404
- TypeScript compilation passes
- Build completes successfully
</success_criteria>

<output>
After completion, create `.planning/phases/03-lead-capture-forms/03-05-SUMMARY.md`
</output>
