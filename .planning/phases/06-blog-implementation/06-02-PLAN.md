---
phase: 06-blog-implementation
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - src/components/blog/BlogCard.tsx
  - src/components/blog/BlogGrid.tsx
  - src/components/blog/CategoryFilter.tsx
  - src/components/blog/index.ts
  - src/app/blog/page.tsx
autonomous: true

must_haves:
  truths:
    - "Visitor sees blog listing page at /blog with all posts in 2-3 column grid"
    - "Visitor can filter posts by category via URL search params"
    - "Blog cards show title, excerpt, date, category, and read time"
  artifacts:
    - path: "src/components/blog/BlogCard.tsx"
      provides: "Reusable blog post card component"
      min_lines: 40
    - path: "src/components/blog/BlogGrid.tsx"
      provides: "Responsive grid layout for blog cards"
      contains: "grid md:grid-cols-2 lg:grid-cols-3"
    - path: "src/components/blog/CategoryFilter.tsx"
      provides: "Client component for category filtering"
      contains: "'use client'"
    - path: "src/app/blog/page.tsx"
      provides: "Blog listing page with searchParams filtering"
      contains: "searchParams"
  key_links:
    - from: "src/app/blog/page.tsx"
      to: "src/lib/blog.ts"
      via: "import getAllPosts"
      pattern: "import.*getAllPosts.*from.*lib/blog"
    - from: "src/components/blog/CategoryFilter.tsx"
      to: "next/navigation"
      via: "useRouter, useSearchParams"
      pattern: "useRouter.*useSearchParams"
---

<objective>
Create blog components and listing page with category filtering.

Purpose: Enable visitors to browse all blog posts in a responsive grid and filter by category using URL-based filtering (server-rendered).

Output: BlogCard, BlogGrid, CategoryFilter components and /blog listing page with searchParams-based filtering.
</objective>

<execution_context>
@/Users/yrs/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yrs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-blog-implementation/06-RESEARCH.md
@.planning/phases/06-blog-implementation/06-01-SUMMARY.md

References:
- src/lib/blog.ts - blog utilities from 06-01
- src/components/ui/card.tsx - existing Card component
- src/components/motion/fade-in.tsx - animation wrapper
- src/components/sections/case-studies.tsx - similar card layout pattern
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create BlogCard component</name>
  <files>src/components/blog/BlogCard.tsx</files>
  <action>
Create src/components/blog/BlogCard.tsx:
- Server Component (no 'use client')
- Import Link from 'next/link'
- Import Image from 'next/image'
- Import { format } from 'date-fns'
- Import Card from '@/components/ui/card' (use CardContent, CardHeader)

Props interface BlogCardProps:
- slug: string
- title: string
- description: string
- date: string
- category: string
- readTime: number
- image: string

Component structure:
- Link wrapper to /blog/{slug} with group class
- Card with hover:shadow-lg transition-shadow
- Image section: relative h-48 w-full overflow-hidden
  - Use Next.js Image with fill, object-cover
  - Add group-hover:scale-105 transition-transform
- Content section:
  - Meta row: category (text-primary), date (formatted with date-fns), readTime
  - Title: text-xl font-semibold mb-2 group-hover:text-primary transition-colors
  - Description: text-muted-foreground line-clamp-3
  - "Read more →" link indicator

Use existing Card component styling patterns from shadcn/ui.
  </action>
  <verify>TypeScript compiles: npx tsc --noEmit</verify>
  <done>BlogCard component renders post preview with hover effects and metadata</done>
</task>

<task type="auto">
  <name>Task 2: Create BlogGrid and CategoryFilter components</name>
  <files>src/components/blog/BlogGrid.tsx, src/components/blog/CategoryFilter.tsx, src/components/blog/index.ts</files>
  <action>
Create src/components/blog/BlogGrid.tsx:
- Server Component
- Import BlogCard
- Import { FadeIn } from '@/components/motion'
- Import { BlogPost } from '@/lib/blog'

Props: posts: BlogPost[]

Render:
- grid gap-6 md:grid-cols-2 lg:grid-cols-3
- Map posts to BlogCard with FadeIn wrapper (staggered delay: index * 0.1)
- Handle empty state: "No posts found" message

Create src/components/blog/CategoryFilter.tsx:
- 'use client' directive
- Import useRouter, useSearchParams from 'next/navigation'
- Import { Button } from '@/components/ui/button'

Props:
- categories: string[]
- selectedCategory?: string

Component:
- Flex container with gap-2 flex-wrap mb-8
- "All" button (active when no selectedCategory)
- Map categories to buttons
- Active state: bg-primary text-primary-foreground (use Button variant="default")
- Inactive state: variant="outline"
- onClick updates URL params via router.push(`/blog?category=${category}`)
- "All" clears the category param

Create src/components/blog/index.ts:
- Export BlogCard, BlogGrid, CategoryFilter
  </action>
  <verify>TypeScript compiles: npx tsc --noEmit</verify>
  <done>Blog grid renders cards in responsive layout, filter updates URL params</done>
</task>

<task type="auto">
  <name>Task 3: Create blog listing page</name>
  <files>src/app/blog/page.tsx</files>
  <action>
Create src/app/blog/page.tsx:
- Server Component
- Import { getAllPosts, getAllCategories } from '@/lib/blog'
- Import { BlogGrid, CategoryFilter } from '@/components/blog'
- Import { FadeIn } from '@/components/motion'

Export metadata:
- title: "Blog | RomAIx"
- description: "Insights on AI automation, workflow optimization, and travel industry technology"

Props interface:
- searchParams: Promise<{ category?: string }>

Async component:
- Await searchParams to get category
- Get all posts and categories
- Filter posts by category if provided
- Return layout:
  - Container: container mx-auto px-4 py-12
  - Header with FadeIn:
    - h1: "Blog" or "Blog: {category}" if filtered
    - Subtitle describing content
  - CategoryFilter with categories and selectedCategory
  - BlogGrid with filtered posts
  - Show count: "{n} posts" or "No posts in this category"

Handle edge cases:
- Empty category returns all posts
- Invalid category shows "No posts found"
  </action>
  <verify>npm run dev and visit /blog - page renders without error</verify>
  <done>/blog page displays posts with working category filtering via URL params</done>
</task>

</tasks>

<verification>
1. /blog route accessible in browser
2. BlogCard shows all metadata (title, description, date, category, read time)
3. Grid is responsive (1 col mobile, 2 col tablet, 3 col desktop)
4. Category filter buttons appear and update URL
5. Filtering by category works via ?category=X param
6. Empty state shown when no posts match filter
</verification>

<success_criteria>
- Blog listing page renders at /blog
- Category filtering works via URL search params
- Cards display all required metadata
- Responsive grid layout (2-3 columns)
- Animation wrappers applied for scroll-triggered entrance
</success_criteria>

<output>
After completion, create `.planning/phases/06-blog-implementation/06-02-SUMMARY.md`
</output>
