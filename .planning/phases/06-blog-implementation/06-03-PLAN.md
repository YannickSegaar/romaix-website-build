---
phase: 06-blog-implementation
plan: 03
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - src/app/blog/[slug]/page.tsx
  - src/components/sections/blog-preview.tsx
  - src/components/sections/index.ts
  - src/app/page.tsx
autonomous: true

must_haves:
  truths:
    - "Visitor can click blog card to read full post content at /blog/{slug}"
    - "Blog post page shows formatted MDX content with prose styling"
    - "Homepage shows latest 3 blog posts in card layout"
  artifacts:
    - path: "src/app/blog/[slug]/page.tsx"
      provides: "Dynamic blog post detail page"
      contains: "generateStaticParams"
    - path: "src/components/sections/blog-preview.tsx"
      provides: "Homepage blog preview section"
      min_lines: 30
    - path: "src/app/page.tsx"
      provides: "Homepage with blog preview section"
      contains: "BlogPreview"
  key_links:
    - from: "src/app/blog/[slug]/page.tsx"
      to: "src/lib/blog.ts"
      via: "import getPostBySlug"
      pattern: "import.*getPostBySlug.*from.*lib/blog"
    - from: "src/app/blog/[slug]/page.tsx"
      to: "dynamic import MDX"
      via: "import MDX file"
      pattern: "await import.*content/posts"
    - from: "src/components/sections/blog-preview.tsx"
      to: "src/lib/blog.ts"
      via: "import getAllPosts"
      pattern: "import.*getAllPosts.*from.*lib/blog"
---

<objective>
Create blog post detail page and homepage blog preview section.

Purpose: Enable visitors to read full blog posts with formatted content, and see latest posts prominently on homepage to drive blog engagement.

Output: Dynamic [slug] route for individual posts with MDX rendering, BlogPreview section on homepage showing latest 3 posts.
</objective>

<execution_context>
@/Users/yrs/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yrs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-blog-implementation/06-RESEARCH.md
@.planning/phases/06-blog-implementation/06-01-SUMMARY.md

References:
- src/lib/blog.ts - blog utilities from 06-01
- mdx-components.tsx - MDX customization from 06-01
- src/app/page.tsx - current homepage structure
- src/components/sections/ - existing section pattern
- src/app/privacy/page.tsx - prose styling reference from Phase 5
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create blog post detail page</name>
  <files>src/app/blog/[slug]/page.tsx</files>
  <action>
Create src/app/blog/[slug]/page.tsx:
- Server Component
- Import { getPostBySlug, getAllPosts } from '@/lib/blog'
- Import { notFound } from 'next/navigation'
- Import { format } from 'date-fns'
- Import { FadeIn } from '@/components/motion'
- Import Link from 'next/link'
- Import { ArrowLeft } from 'lucide-react'

Props interface:
- params: Promise<{ slug: string }>

Export generateStaticParams():
- Get all posts
- Return array of { slug: post.slug }

Export dynamicParams = false (404 for unknown slugs)

Export generateMetadata({ params }):
- Await params
- Get post by slug
- Return { title: post.title + " | RomAIx Blog", description: post.description }
- Return empty object if not found

Async component:
- Await params to get slug
- Get post by slug
- If not found, call notFound()
- Dynamic import MDX: const { default: MDXContent } = await import(`@/content/posts/${slug}.mdx`)

Render:
- Container: container mx-auto px-4 py-12
- Back link: Link to /blog with ArrowLeft icon "Back to Blog"
- Article wrapper with FadeIn
- Header section:
  - Category badge (text-sm text-primary bg-primary/10 px-3 py-1 rounded-full)
  - h1: text-4xl font-bold mb-4
  - Meta row: date (formatted), author, readTime + " min read"
- Feature image if post.image exists:
  - relative aspect-video w-full mb-8 rounded-lg overflow-hidden
  - Next.js Image with fill, object-cover
- Content: prose prose-slate lg:prose-lg dark:prose-invert mx-auto max-w-4xl
  - <MDXContent />
- Tags section at bottom if post.tags.length > 0:
  - Flex wrap with tag badges
  </action>
  <verify>npm run dev and verify /blog/[slug] routes work (will need test post in 06-04)</verify>
  <done>Blog post detail page renders MDX content with full metadata and prose styling</done>
</task>

<task type="auto">
  <name>Task 2: Create BlogPreview section and wire to homepage</name>
  <files>src/components/sections/blog-preview.tsx, src/components/sections/index.ts, src/app/page.tsx</files>
  <action>
Create src/components/sections/blog-preview.tsx:
- Server Component
- Import { getAllPosts } from '@/lib/blog'
- Import { BlogCard } from '@/components/blog'
- Import { FadeIn } from '@/components/motion'
- Import Link from 'next/link'
- Import { Button } from '@/components/ui/button'
- Import { ArrowRight } from 'lucide-react'

Export function BlogPreview():
- Get latest 3 posts: getAllPosts().slice(0, 3)
- If no posts, return null (section won't render until posts exist)

Render:
- Section: py-20 bg-muted/30 (consistent with HowItWorks background pattern)
- Container: container mx-auto px-4
- Header with FadeIn:
  - h2: "Latest Insights" (text-3xl font-bold mb-4)
  - Subtitle: "Explore our thoughts on AI automation and the travel industry"
- Grid: grid md:grid-cols-2 lg:grid-cols-3 gap-6 mb-8
  - Map posts to BlogCard (limit to 3)
  - Wrap each in FadeIn with staggered delay
- CTA with FadeIn:
  - Center aligned
  - Button variant="outline" with Link to /blog
  - "View all posts" with ArrowRight icon

Update src/components/sections/index.ts:
- Add: export { BlogPreview } from './blog-preview'

Update src/app/page.tsx:
- Import BlogPreview from sections
- Add <BlogPreview /> after FAQ section (before Contact)
  </action>
  <verify>npm run dev and verify homepage loads (BlogPreview will be hidden until posts exist)</verify>
  <done>Homepage includes BlogPreview section showing latest 3 posts when available</done>
</task>

</tasks>

<verification>
1. /blog/[slug] route renders MDX content when post exists
2. generateStaticParams returns all post slugs
3. 404 returned for non-existent slugs (dynamicParams = false)
4. Post page shows title, date, author, read time, category
5. Prose styling applied to MDX content
6. BlogPreview section on homepage (visible once posts exist)
7. "View all posts" link navigates to /blog
</verification>

<success_criteria>
- Individual blog posts render at /blog/{slug}
- MDX content displays with prose styling
- Metadata (SEO) generated for each post
- Homepage shows latest 3 posts when available
- BlogPreview gracefully handles no-posts state
</success_criteria>

<output>
After completion, create `.planning/phases/06-blog-implementation/06-03-SUMMARY.md`
</output>
