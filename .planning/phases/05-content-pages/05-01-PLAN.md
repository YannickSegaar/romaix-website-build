---
phase: 05-content-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/globals.css
  - src/app/about/page.tsx
autonomous: true

must_haves:
  truths:
    - "Visitor can navigate to /about and see RomAIx mission and travel industry focus"
    - "About page displays team section with placeholder member cards"
    - "@tailwindcss/typography plugin is installed and available for prose classes"
  artifacts:
    - path: "src/app/about/page.tsx"
      provides: "About page with mission, team section, travel focus"
      min_lines: 80
    - path: "src/app/globals.css"
      provides: "Typography plugin import"
      contains: "@plugin"
  key_links:
    - from: "src/components/layout/header.tsx"
      to: "/about"
      via: "navLinks array"
      pattern: "href.*about"
    - from: "src/components/layout/footer.tsx"
      to: "/about"
      via: "footerLinks.company"
      pattern: "href.*about"
---

<objective>
Install @tailwindcss/typography plugin and create the About page with company mission, travel industry focus, and team section.

Purpose: Establish company credibility and help visitors understand RomAIx's expertise in travel industry AI automation.
Output: Typography plugin installed, working /about page with mission statement, value proposition, and team section.
</objective>

<execution_context>
@/Users/yrs/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yrs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-content-pages/05-RESEARCH.md
@src/app/globals.css
@src/components/layout/header.tsx
@src/components/layout/footer.tsx
@src/components/motion/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install typography plugin and configure</name>
  <files>src/app/globals.css, package.json</files>
  <action>
1. Install @tailwindcss/typography plugin:
   ```bash
   npm install @tailwindcss/typography
   ```

2. Add plugin import to globals.css after the @import "tailwindcss" line:
   ```css
   @plugin "@tailwindcss/typography";
   ```
   This is the Tailwind v4 CSS-based configuration syntax.

3. Verify installation by checking package.json includes @tailwindcss/typography.

Note: Do NOT create tailwind.config.js - the project uses Tailwind v4 with CSS-based configuration.
  </action>
  <verify>Run `npm run build` - should complete without errors. Check package.json shows @tailwindcss/typography in dependencies.</verify>
  <done>Typography plugin installed and configured via CSS import.</done>
</task>

<task type="auto">
  <name>Task 2: Create About page with mission and team</name>
  <files>src/app/about/page.tsx</files>
  <action>
Create src/app/about/page.tsx as a Server Component with:

1. **Static metadata export** (not generateMetadata):
   ```typescript
   export const metadata: Metadata = {
     title: 'About Us | RomAIx',
     description: 'Learn about RomAIx team and our mission to transform the travel industry with AI automation solutions.',
     openGraph: {
       title: 'About RomAIx - AI Automation for Travel Industry',
       description: 'Meet the team behind RomAIx',
       type: 'website',
     },
   }
   ```

2. **Page structure with sections:**
   - Hero section: "About RomAIx" heading with mission statement
   - Mission section: Travel industry focus, why we exist, what we do
   - Values section: 3 core values (e.g., Innovation, Travel Expertise, Results-Driven)
   - Team section: Grid of 3 team member cards with placeholder content

3. **Team member data structure:**
   ```typescript
   const teamMembers = [
     { name: 'Team Member', role: 'Founder & CEO', initials: 'TM' },
     { name: 'Team Member', role: 'CTO', initials: 'TM' },
     { name: 'Team Member', role: 'Head of Operations', initials: 'TM' },
   ]
   ```

4. **Styling requirements:**
   - Use container px-4 md:px-6 py-16 md:py-24 (matches existing pattern)
   - Use FadeIn wrapper for scroll animations
   - Team cards: rounded avatar placeholder, name, role
   - Values: icon or number + title + description grid
   - Use text-muted-foreground for secondary text
   - Use bg-muted/30 for card backgrounds

5. **Content to include:**
   - Mission: "Transforming travel businesses with intelligent automation"
   - Focus: Tour operators, travel agencies, boutique hotels
   - Value prop: Save time, reduce costs, improve guest experience
   - Placeholder team bios ready for real content

Import FadeIn from @/components/motion. Use Lucide icons if needed (already installed).
  </action>
  <verify>Run `npm run dev`, navigate to http://localhost:3000/about - page renders with all sections, no console errors, FadeIn animations work on scroll.</verify>
  <done>About page displays mission, values, and team section with consistent styling and scroll animations.</done>
</task>

</tasks>

<verification>
1. `npm run build` completes without errors
2. Navigate to /about - page renders with header/footer
3. Mission and values sections display correctly
4. Team section shows 3 placeholder cards
5. FadeIn animations trigger on scroll
6. Header "About" link works (already linked)
7. Footer "About" link works (already linked)
8. Mobile responsive (test at 375px width)
</verification>

<success_criteria>
- Typography plugin installed and importable via prose classes
- /about page renders with mission statement focusing on travel industry AI
- Team section displays grid of 3 placeholder member cards
- Page uses consistent container/padding pattern from existing pages
- FadeIn scroll animations work
- No TypeScript or build errors
</success_criteria>

<output>
After completion, create `.planning/phases/05-content-pages/05-01-SUMMARY.md`
</output>
