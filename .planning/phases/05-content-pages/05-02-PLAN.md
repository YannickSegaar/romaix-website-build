---
phase: 05-content-pages
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/contact/page.tsx
autonomous: true

must_haves:
  truths:
    - "Visitor sees contact information (email, response time) alongside the form"
    - "Contact page displays two-column layout on desktop (info + form)"
    - "Page maintains conditional form rendering (demo, assessment, contact)"
  artifacts:
    - path: "src/app/contact/page.tsx"
      provides: "Enhanced contact page with info sidebar"
      min_lines: 80
  key_links:
    - from: "src/app/contact/page.tsx"
      to: "@/components/forms"
      via: "imports ContactForm, AssessmentForm, DemoForm"
      pattern: "import.*forms"
---

<objective>
Enhance the existing Contact page with contact information context, creating a two-column layout with company info alongside the form.

Purpose: Improve contact page UX by providing alternative contact methods and setting expectations (response time).
Output: Enhanced /contact page with info sidebar maintaining existing form functionality.
</objective>

<execution_context>
@/Users/yrs/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yrs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-content-pages/05-RESEARCH.md
@src/app/contact/page.tsx
@src/components/forms/index.ts
@src/components/motion/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance Contact page with info sidebar</name>
  <files>src/app/contact/page.tsx</files>
  <action>
Refactor src/app/contact/page.tsx to add contact information while preserving existing functionality:

1. **Keep existing functionality intact:**
   - searchParams Promise handling for form type
   - Conditional form rendering (demo, assessment, contact)
   - Dynamic heading/subheading based on form type
   - FadeIn animations

2. **Add two-column layout on desktop:**
   ```tsx
   <div className="grid md:grid-cols-2 gap-12 max-w-6xl mx-auto">
     {/* Left: Contact Information */}
     <div>...</div>

     {/* Right: Form */}
     <div className="bg-muted/30 p-6 md:p-8 rounded-lg">
       {/* existing form rendering */}
     </div>
   </div>
   ```

3. **Contact information section (left column):**
   - Heading and subheading (existing, moved here)
   - Email: hello@romaix.com (placeholder - easy to update)
   - Response time: "Within 24 hours"
   - Office hours: "Monday - Friday, 9am - 6pm CET"
   - Location hint: "Based in Europe, serving clients worldwide"

4. **Styling requirements:**
   - Use FadeIn for left column content
   - Use Lucide icons for each info item (Mail, Clock, MapPin, Calendar)
   - Info items: icon + label + value stacked
   - Space-y-6 for info items grouping
   - On mobile: stack columns (form below info)

5. **Update metadata to be more comprehensive:**
   ```typescript
   export const metadata: Metadata = {
     title: 'Contact Us | RomAIx',
     description: 'Get in touch with RomAIx for AI automation solutions. Book a demo, request a free assessment, or send us a message.',
     openGraph: {
       title: 'Contact RomAIx',
       description: 'AI automation solutions for travel industry',
       type: 'website',
     },
   }
   ```

6. **Preserve form type switching:**
   - /contact shows ContactForm with general info heading
   - /contact?type=demo shows DemoForm with "Book a Demo" heading
   - /contact?type=assessment shows AssessmentForm with assessment heading
   - Info sidebar content remains the same for all form types

Import icons from lucide-react: Mail, Clock, MapPin, Calendar (or Globe).
  </action>
  <verify>
1. Navigate to http://localhost:3000/contact - shows two-column layout with info + ContactForm
2. Navigate to /contact?type=demo - shows info + DemoForm with "Book a Demo" heading
3. Navigate to /contact?type=assessment - shows info + AssessmentForm
4. Test at 375px width - columns stack properly
5. No console errors
  </verify>
  <done>Contact page displays two-column layout with contact information and preserves conditional form rendering.</done>
</task>

</tasks>

<verification>
1. `npm run build` completes without errors
2. /contact renders with two-column layout on desktop
3. Contact info (email, response time) visible in left column
4. Form renders in right column with bg-muted/30 background
5. /contact?type=demo correctly shows DemoForm
6. /contact?type=assessment correctly shows AssessmentForm
7. Mobile layout stacks properly (info above form)
8. Icons display correctly for each info item
9. FadeIn animations work
</verification>

<success_criteria>
- Contact page shows contact information alongside form
- Two-column layout on desktop, stacked on mobile
- All three form types still work correctly
- Response time and email clearly visible
- Consistent styling with rest of site
- No TypeScript or build errors
</success_criteria>

<output>
After completion, create `.planning/phases/05-content-pages/05-02-SUMMARY.md`
</output>
