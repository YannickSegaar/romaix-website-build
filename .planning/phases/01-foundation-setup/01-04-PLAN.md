---
phase: 01-foundation-setup
plan: 04
type: execute
wave: 3
depends_on: ["01-02", "01-03"]
files_modified:
  - src/components/layout/header.tsx
  - src/components/layout/footer.tsx
  - src/components/layout/index.ts
  - src/app/layout.tsx
  - src/app/page.tsx
autonomous: false

must_haves:
  truths:
    - "Developer sees header with RomAIx logo and navigation on every page"
    - "Developer sees footer with links and copyright on every page"
    - "Website displays correctly on mobile (375px viewport)"
    - "Website displays correctly on tablet (768px viewport)"
    - "Website displays correctly on desktop (1440px viewport)"
    - "Navigation collapses to mobile menu on small screens"
  artifacts:
    - path: "src/components/layout/header.tsx"
      provides: "Responsive header with logo, nav, and CTAs"
      exports: ["Header"]
    - path: "src/components/layout/footer.tsx"
      provides: "Footer with links and copyright"
      exports: ["Footer"]
    - path: "src/app/layout.tsx"
      provides: "Root layout with Header and Footer"
      contains: "Header"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/components/layout/header.tsx"
      via: "import and render"
      pattern: "import.*Header.*from"
    - from: "src/components/layout/header.tsx"
      to: "src/components/ui/button.tsx"
      via: "CTA buttons"
      pattern: "import.*Button.*from.*@/components/ui"
---

<objective>
Create responsive marketing layout with Header and Footer components integrated into the root layout.

Purpose: Establishes the site shell that wraps all page content. Header provides navigation and CTAs, Footer provides links and company info. Both must work across all viewport sizes (mobile-first approach).

Output: Root layout with persistent Header and Footer that display correctly on mobile (375px), tablet (768px), and desktop (1440px) viewports.
</objective>

<execution_context>
@/Users/yrs/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yrs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-foundation-setup/01-RESEARCH.md
@.planning/phases/01-foundation-setup/01-02-SUMMARY.md
@.planning/phases/01-foundation-setup/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create responsive Header component</name>
  <files>
    src/components/layout/header.tsx
  </files>
  <action>
Create responsive Header with desktop nav and mobile menu:

```tsx
// src/components/layout/header.tsx
'use client'

import Link from 'next/link'
import { useState } from 'react'
import { Button } from '@/components/ui/button'
import { Menu, X } from 'lucide-react'

const navLinks = [
  { href: '/about', label: 'About' },
  { href: '/case-studies', label: 'Case Studies' },
  { href: '/blog', label: 'Blog' },
  { href: '/contact', label: 'Contact' },
]

export function Header() {
  const [mobileMenuOpen, setMobileMenuOpen] = useState(false)

  return (
    <header className="sticky top-0 z-50 w-full border-b bg-background/95 backdrop-blur supports-[backdrop-filter]:bg-background/60">
      <div className="container flex h-16 items-center justify-between">
        {/* Logo */}
        <Link href="/" className="text-xl font-bold text-primary">
          RomAIx
        </Link>

        {/* Desktop Navigation */}
        <nav className="hidden md:flex items-center gap-6">
          {navLinks.map((link) => (
            <Link
              key={link.href}
              href={link.href}
              className="text-sm font-medium text-muted-foreground hover:text-primary transition-colors"
            >
              {link.label}
            </Link>
          ))}
        </nav>

        {/* Desktop CTAs */}
        <div className="hidden md:flex items-center gap-2">
          <Button variant="ghost" asChild>
            <Link href="/contact">Get Assessment</Link>
          </Button>
          <Button asChild>
            <Link href="/contact?type=demo">Book Demo</Link>
          </Button>
        </div>

        {/* Mobile Menu Button */}
        <button
          className="md:hidden p-2"
          onClick={() => setMobileMenuOpen(!mobileMenuOpen)}
          aria-label={mobileMenuOpen ? 'Close menu' : 'Open menu'}
        >
          {mobileMenuOpen ? <X className="h-6 w-6" /> : <Menu className="h-6 w-6" />}
        </button>
      </div>

      {/* Mobile Menu */}
      {mobileMenuOpen && (
        <div className="md:hidden border-t bg-background">
          <nav className="container py-4 flex flex-col gap-4">
            {navLinks.map((link) => (
              <Link
                key={link.href}
                href={link.href}
                className="text-sm font-medium text-muted-foreground hover:text-primary transition-colors"
                onClick={() => setMobileMenuOpen(false)}
              >
                {link.label}
              </Link>
            ))}
            <div className="flex flex-col gap-2 pt-4 border-t">
              <Button variant="outline" asChild className="w-full">
                <Link href="/contact">Get Free Assessment</Link>
              </Button>
              <Button asChild className="w-full">
                <Link href="/contact?type=demo">Book a Demo</Link>
              </Button>
            </div>
          </nav>
        </div>
      )}
    </header>
  )
}
```

Install lucide-react for icons:

```bash
npm install lucide-react
```

CRITICAL: Use 'use client' because of useState for mobile menu toggle.
Mobile-first: Hide desktop nav with `hidden md:flex`, show mobile button without prefix.
Do NOT use a heavy UI library for mobile menu - simple state toggle is sufficient.
  </action>
  <verify>
1. File exists: src/components/layout/header.tsx
2. lucide-react is in package.json
3. Run `npm run dev` and import Header in layout - no errors
  </verify>
  <done>
Header component is created with responsive navigation, mobile menu toggle, and CTA buttons. Uses Tailwind mobile-first responsive classes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Footer component and barrel export</name>
  <files>
    src/components/layout/footer.tsx
    src/components/layout/index.ts
  </files>
  <action>
Create Footer component:

```tsx
// src/components/layout/footer.tsx
import Link from 'next/link'

const footerLinks = {
  company: [
    { href: '/about', label: 'About' },
    { href: '/case-studies', label: 'Case Studies' },
    { href: '/blog', label: 'Blog' },
    { href: '/contact', label: 'Contact' },
  ],
  legal: [
    { href: '/privacy', label: 'Privacy Policy' },
    { href: '/terms', label: 'Terms of Service' },
  ],
}

export function Footer() {
  return (
    <footer className="border-t bg-muted/50">
      <div className="container py-12 md:py-16">
        <div className="grid gap-8 md:grid-cols-4">
          {/* Brand */}
          <div className="md:col-span-2">
            <Link href="/" className="text-xl font-bold text-primary">
              RomAIx
            </Link>
            <p className="mt-4 text-sm text-muted-foreground max-w-xs">
              AI automation solutions for tour operators, travel agencies, and boutique hotels.
            </p>
          </div>

          {/* Company Links */}
          <div>
            <h3 className="font-semibold mb-4">Company</h3>
            <ul className="space-y-2">
              {footerLinks.company.map((link) => (
                <li key={link.href}>
                  <Link
                    href={link.href}
                    className="text-sm text-muted-foreground hover:text-primary transition-colors"
                  >
                    {link.label}
                  </Link>
                </li>
              ))}
            </ul>
          </div>

          {/* Legal Links */}
          <div>
            <h3 className="font-semibold mb-4">Legal</h3>
            <ul className="space-y-2">
              {footerLinks.legal.map((link) => (
                <li key={link.href}>
                  <Link
                    href={link.href}
                    className="text-sm text-muted-foreground hover:text-primary transition-colors"
                  >
                    {link.label}
                  </Link>
                </li>
              ))}
            </ul>
          </div>
        </div>

        {/* Copyright */}
        <div className="mt-12 pt-8 border-t text-center text-sm text-muted-foreground">
          <p>&copy; {new Date().getFullYear()} RomAIx. All rights reserved.</p>
        </div>
      </div>
    </footer>
  )
}
```

Create barrel export:

```tsx
// src/components/layout/index.ts
export { Header } from './header'
export { Footer } from './footer'
```

Footer is a Server Component (no 'use client' needed - no interactivity).
Uses grid for responsive layout: stacks on mobile, 4-column on md+.
  </action>
  <verify>
1. File exists: src/components/layout/footer.tsx
2. File exists: src/components/layout/index.ts
3. Both export correctly: `import { Header, Footer } from '@/components/layout'`
  </verify>
  <done>
Footer component is created with responsive grid layout, navigation links, and copyright. Barrel export enables clean imports.
  </done>
</task>

<task type="auto">
  <name>Task 3: Integrate Header and Footer into root layout</name>
  <files>
    src/app/layout.tsx
    src/app/page.tsx
  </files>
  <action>
Update root layout to include Header and Footer:

```tsx
// src/app/layout.tsx
import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'
import { Header, Footer } from '@/components/layout'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'RomAIx - AI Automation for Travel Industry',
  description: 'Custom AI solutions for tour operators, travel agencies, and boutique hotels. Automate customer service, bookings, and workflows.',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <Header />
        <main className="min-h-[calc(100vh-4rem)]">{children}</main>
        <Footer />
      </body>
    </html>
  )
}
```

Create a simple homepage to test the layout shell:

```tsx
// src/app/page.tsx
import { Button } from "@/components/ui/button"
import { FadeIn } from "@/components/motion"

export default function Home() {
  return (
    <div className="container py-16 md:py-24">
      <FadeIn>
        <section className="text-center max-w-3xl mx-auto">
          <h1 className="text-4xl md:text-5xl lg:text-6xl font-bold tracking-tight">
            AI Automation for the{' '}
            <span className="text-primary">Travel Industry</span>
          </h1>
          <p className="mt-6 text-lg text-muted-foreground">
            Custom AI solutions for tour operators, travel agencies, and boutique hotels.
            Automate customer service, bookings, and workflows.
          </p>
          <div className="mt-8 flex flex-col sm:flex-row gap-4 justify-center">
            <Button size="lg">Book a Demo</Button>
            <Button size="lg" variant="outline">Get Free Assessment</Button>
          </div>
        </section>
      </FadeIn>
    </div>
  )
}
```

The layout uses:
- Inter font (clean, modern sans-serif)
- Header at top (sticky)
- Main content area with min-height to push footer down
- Footer at bottom
  </action>
  <verify>
1. Run `npm run dev`
2. Visit localhost:3000
3. Header appears at top with RomAIx logo and navigation
4. Footer appears at bottom with links and copyright
5. Content appears between header and footer
6. Run `npm run build` - no errors
  </verify>
  <done>
Root layout includes Header and Footer. Homepage displays simple hero content. Layout structure is complete.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete responsive layout shell with Header (desktop nav + mobile menu) and Footer, plus animation infrastructure.

The foundation phase is complete with:
- Next.js 15 project with TypeScript, Tailwind, ESLint
- shadcn/ui components (Button, Card, Input, Label) with brand color #587C74
- Framer Motion wrappers (FadeIn, SlideIn, StaggerContainer)
- Responsive Header with mobile menu
- Responsive Footer with links
  </what-built>
  <how-to-verify>
1. Open http://localhost:3000

2. Desktop verification (resize browser to 1440px width):
   - Header shows: RomAIx logo, nav links (About, Case Studies, Blog, Contact), two CTA buttons
   - Hero section centered with heading, description, two buttons
   - Footer shows: brand, company links, legal links, copyright
   - Scroll animations work (if content requires scrolling)

3. Tablet verification (resize to 768px width):
   - Layout adjusts proportionally
   - Header still shows desktop nav (md breakpoint)
   - Footer grid collapses appropriately

4. Mobile verification (resize to 375px width):
   - Header shows: RomAIx logo, hamburger menu icon
   - Tap hamburger: mobile menu slides down with nav links + CTAs
   - Tap X or link: menu closes
   - Footer stacks vertically
   - Hero text is readable, buttons stack vertically

5. Brand color verification:
   - Primary buttons have sage/teal background (#587C74)
   - "Travel Industry" text in hero is sage/teal
   - Hover states use brand color appropriately
  </how-to-verify>
  <resume-signal>Type "approved" if layout looks correct across all breakpoints, or describe any issues to fix.</resume-signal>
</task>

</tasks>

<verification>
Full verification sequence:

1. `npm run dev` starts without errors
2. Header visible on all pages with navigation and CTAs
3. Footer visible on all pages with links and copyright
4. Mobile menu opens/closes correctly
5. Layout works at 375px, 768px, and 1440px viewports
6. Brand color #587C74 visible in buttons and accents
7. FadeIn animation works on hero section
8. `npm run build` passes without errors
</verification>

<success_criteria>
- Header with logo, navigation, and CTAs renders on every page
- Footer with links and copyright renders on every page
- Mobile menu toggles open/closed
- Layout is responsive across mobile (375px), tablet (768px), desktop (1440px)
- Brand color applied consistently
- Animations work (FadeIn on hero)
- Build completes without errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-setup/01-04-SUMMARY.md`
</output>
