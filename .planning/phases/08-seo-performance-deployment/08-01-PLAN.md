---
phase: 08-seo-performance-deployment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/constants.ts
  - src/app/layout.tsx
  - src/app/sitemap.ts
  - src/app/robots.ts
  - src/app/page.tsx
  - src/app/about/page.tsx
  - src/app/contact/page.tsx
  - src/app/privacy/page.tsx
  - src/app/terms/page.tsx
  - src/app/blog/page.tsx
  - src/app/case-studies/page.tsx
autonomous: true

must_haves:
  truths:
    - "Every static page has unique title and meta description"
    - "metadataBase is set enabling absolute OpenGraph URLs"
    - "sitemap.xml is accessible at /sitemap.xml with all pages"
    - "robots.txt is accessible at /robots.txt with correct rules"
  artifacts:
    - path: "src/lib/constants.ts"
      provides: "SITE_URL and metadata defaults"
      contains: "SITE_URL"
    - path: "src/app/sitemap.ts"
      provides: "Dynamic sitemap generation"
      exports: ["default"]
    - path: "src/app/robots.ts"
      provides: "robots.txt configuration"
      exports: ["default"]
    - path: "src/app/layout.tsx"
      provides: "Root metadata with metadataBase"
      contains: "metadataBase"
  key_links:
    - from: "src/app/sitemap.ts"
      to: "src/lib/blog.ts"
      via: "imports getAllPosts"
      pattern: "getAllPosts"
    - from: "src/app/sitemap.ts"
      to: "src/lib/case-studies.ts"
      via: "imports getAllCaseStudies"
      pattern: "getAllCaseStudies"
---

<objective>
Establish SEO foundation with metadataBase, static page metadata, sitemap, and robots.txt

Purpose: Enable search engine discovery and proper social sharing URLs. Without metadataBase, all OpenGraph images use relative URLs that social platforms cannot resolve.

Output: Constants file, updated root layout with metadataBase, static page metadata exports, sitemap.ts, robots.ts
</objective>

<execution_context>
@/Users/yrs/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yrs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-seo-performance-deployment/08-RESEARCH.md

@src/app/layout.tsx
@src/lib/blog.ts
@src/lib/case-studies.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create constants and update root layout with metadataBase</name>
  <files>
    src/lib/constants.ts
    src/app/layout.tsx
  </files>
  <action>
1. Create src/lib/constants.ts with:
   - SITE_URL: `process.env.NEXT_PUBLIC_SITE_URL || 'https://romaix.ai'`
   - SITE_NAME: 'RomAIx'
   - DEFAULT_DESCRIPTION: 'Custom AI solutions for tour operators, travel agencies, and boutique hotels. Automate customer service, bookings, and workflows.'

2. Update src/app/layout.tsx:
   - Import SITE_URL, SITE_NAME, DEFAULT_DESCRIPTION from constants
   - Add metadataBase: `new URL(SITE_URL)`
   - Use title.default and title.template pattern: `{ default: 'RomAIx - AI Automation for Travel Industry', template: '%s | RomAIx' }`
   - Add openGraph defaults (type: 'website', locale: 'en_US', siteName)
   - Add twitter defaults (card: 'summary_large_image')
   - Add robots: { index: true, follow: true }
   - Keep Inter font configuration with variable and display: 'swap' for CLS optimization

IMPORTANT: metadataBase MUST use `new URL()` constructor, not a plain string. This is critical for OpenGraph URLs to resolve correctly.
  </action>
  <verify>
Run `npm run build` - no TypeScript errors. Check generated HTML in .next for `<meta property="og:url">` containing absolute URL.
  </verify>
  <done>
Root layout exports metadata with metadataBase set, title template configured, and OpenGraph/Twitter defaults.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add unique metadata to all static pages</name>
  <files>
    src/app/page.tsx
    src/app/about/page.tsx
    src/app/contact/page.tsx
    src/app/privacy/page.tsx
    src/app/terms/page.tsx
    src/app/blog/page.tsx
    src/app/case-studies/page.tsx
  </files>
  <action>
Add `export const metadata: Metadata` to each page with unique title and description:

1. **Homepage (page.tsx)**: Title uses default from layout. Add description specific to homepage value prop.

2. **About page**: title: 'About Us', description about mission and travel industry focus.

3. **Contact page**: title: 'Contact Us', description about getting in touch for demo/assessment.

4. **Privacy page**: title: 'Privacy Policy', description about data practices.

5. **Terms page**: title: 'Terms of Service', description about service terms.

6. **Blog listing (blog/page.tsx)**: title: 'Blog', description about AI automation insights for travel.

7. **Case Studies listing (case-studies/page.tsx)**: title: 'Case Studies', description about client success stories.

Each page should import `Metadata` type from 'next' and export named `metadata` object. Use title strings (template from layout applies automatically).
  </action>
  <verify>
Run `npm run build`. Visit each page in browser, check document title matches expected format "[Page Title] | RomAIx". View page source to verify unique meta descriptions.
  </verify>
  <done>
All 7 static pages have unique title and description metadata exported.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create sitemap.ts and robots.ts</name>
  <files>
    src/app/sitemap.ts
    src/app/robots.ts
  </files>
  <action>
1. Create src/app/sitemap.ts:
   - Import `MetadataRoute` from 'next'
   - Import `getAllPosts` from '@/lib/blog'
   - Import `getAllCaseStudies` from '@/lib/case-studies'
   - Import `SITE_URL` from '@/lib/constants'
   - Export default async function returning `MetadataRoute.Sitemap`
   - Include static pages: home (priority 1), about (0.8), contact (0.5), blog (0.8), case-studies (0.8), privacy (0.3), terms (0.3)
   - Include dynamic blog posts: priority 0.7, changeFrequency 'monthly'
   - Include dynamic case studies: priority 0.8, changeFrequency 'monthly'
   - Use SITE_URL for absolute URLs

2. Create src/app/robots.ts:
   - Import `MetadataRoute` from 'next'
   - Import `SITE_URL` from '@/lib/constants'
   - Export default function returning `MetadataRoute.Robots`
   - Rules: allow '/', disallow '/api/'
   - Include sitemap URL: `${SITE_URL}/sitemap.xml`
  </action>
  <verify>
Run `npm run dev`. Visit http://localhost:3000/sitemap.xml - should return XML with all pages. Visit http://localhost:3000/robots.txt - should return text with Allow/Disallow rules and Sitemap URL.
  </verify>
  <done>
sitemap.xml includes all static and dynamic pages. robots.txt correctly allows crawling with sitemap reference.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` completes without errors
2. http://localhost:3000/sitemap.xml returns valid XML with 10+ URLs
3. http://localhost:3000/robots.txt shows proper crawl rules
4. Every page has unique `<title>` tag
5. Every page has `<meta name="description">` tag
6. Page source shows `<meta property="og:url">` with absolute https:// URL
</verification>

<success_criteria>
- metadataBase configured in root layout
- All 7 static pages have unique metadata
- sitemap.ts generates XML with static + dynamic pages
- robots.ts generates proper crawl directives
- Build passes without TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/08-seo-performance-deployment/08-01-SUMMARY.md`
</output>
