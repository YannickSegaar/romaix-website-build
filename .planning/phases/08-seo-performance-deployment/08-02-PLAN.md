---
phase: 08-seo-performance-deployment
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - src/app/blog/[slug]/page.tsx
  - src/app/case-studies/[slug]/page.tsx
  - src/app/opengraph-image.tsx
  - src/app/blog/[slug]/opengraph-image.tsx
  - src/app/case-studies/[slug]/opengraph-image.tsx
autonomous: true

must_haves:
  truths:
    - "Blog posts have full OpenGraph metadata with article type and published time"
    - "Case studies have full OpenGraph metadata with article type"
    - "Default OG image generates branded preview for homepage"
    - "Dynamic OG images generate unique previews for each blog post"
    - "Dynamic OG images generate unique previews for each case study"
  artifacts:
    - path: "src/app/blog/[slug]/page.tsx"
      provides: "Enhanced blog generateMetadata with OpenGraph"
      contains: "openGraph"
    - path: "src/app/case-studies/[slug]/page.tsx"
      provides: "Enhanced case study generateMetadata with OpenGraph"
      contains: "openGraph"
    - path: "src/app/opengraph-image.tsx"
      provides: "Default OpenGraph image"
      exports: ["default", "alt", "size", "contentType"]
    - path: "src/app/blog/[slug]/opengraph-image.tsx"
      provides: "Blog post OpenGraph images"
      exports: ["default", "alt", "size", "contentType"]
    - path: "src/app/case-studies/[slug]/opengraph-image.tsx"
      provides: "Case study OpenGraph images"
      exports: ["default", "alt", "size", "contentType"]
  key_links:
    - from: "src/app/blog/[slug]/opengraph-image.tsx"
      to: "src/lib/blog.ts"
      via: "getPostBySlug for content"
      pattern: "getPostBySlug"
    - from: "src/app/case-studies/[slug]/opengraph-image.tsx"
      to: "src/lib/case-studies.ts"
      via: "getCaseStudyBySlug for content"
      pattern: "getCaseStudyBySlug"
---

<objective>
Add comprehensive OpenGraph metadata and dynamic OG image generation for social sharing

Purpose: When users share blog posts or case studies on LinkedIn/Twitter/Facebook, they see rich previews with unique branded images. This dramatically improves click-through rates on social shares.

Output: Enhanced generateMetadata in dynamic routes, opengraph-image.tsx files for default and per-route images
</objective>

<execution_context>
@/Users/yrs/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yrs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08-seo-performance-deployment/08-RESEARCH.md
@.planning/phases/08-seo-performance-deployment/08-01-SUMMARY.md

@src/app/blog/[slug]/page.tsx
@src/app/case-studies/[slug]/page.tsx
@src/lib/constants.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance dynamic route metadata with full OpenGraph</name>
  <files>
    src/app/blog/[slug]/page.tsx
    src/app/case-studies/[slug]/page.tsx
  </files>
  <action>
1. Update src/app/blog/[slug]/page.tsx generateMetadata:
   - Keep existing title/description logic
   - Add openGraph object with:
     - type: 'article'
     - publishedTime: post.date (ISO string)
     - authors: [post.author]
     - title, description (same as page)
     - Images array pointing to generated OG image (relative URL works with metadataBase)
   - Add twitter object with:
     - card: 'summary_large_image'
     - title, description (same as page)

2. Update src/app/case-studies/[slug]/page.tsx generateMetadata:
   - Keep existing title/description logic
   - Add openGraph object with:
     - type: 'article'
     - publishedTime: study.date
     - title, description (same as page)
   - Add twitter object with:
     - card: 'summary_large_image'
     - title, description

NOTE: Do NOT manually set og:image URL - the opengraph-image.tsx files handle this automatically via Next.js file conventions.
  </action>
  <verify>
Run `npm run dev`. Visit a blog post and case study. View page source and search for `og:type` - should show "article". Search for `og:published_time` - should show ISO date.
  </verify>
  <done>
Blog posts and case studies have full OpenGraph metadata with article type, published time, and Twitter card configuration.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create OpenGraph image generators</name>
  <files>
    src/app/opengraph-image.tsx
    src/app/blog/[slug]/opengraph-image.tsx
    src/app/case-studies/[slug]/opengraph-image.tsx
  </files>
  <action>
1. Create src/app/opengraph-image.tsx (default for homepage and static pages):
   - Import `ImageResponse` from 'next/og'
   - Export alt, size (1200x630), contentType ('image/png')
   - Export default function returning ImageResponse
   - Design: Brand color (#587C74) gradient background, "RomAIx" text large, tagline "AI Automation for Travel Industry" below
   - Use inline styles (no Tailwind in OG images)

2. Create src/app/blog/[slug]/opengraph-image.tsx:
   - Import `ImageResponse` from 'next/og'
   - Import `getPostBySlug` from '@/lib/blog'
   - Accept params: Promise<{ slug: string }>
   - Await params, fetch post data
   - Export alt (dynamic based on post title), size (1200x630), contentType
   - Design: Brand gradient background, post.title large (up to 60 chars truncated), post.category badge, "RomAIx Blog" footer

3. Create src/app/case-studies/[slug]/opengraph-image.tsx:
   - Same pattern as blog
   - Import `getCaseStudyBySlug` from '@/lib/case-studies'
   - Design: Brand gradient, study.title, study.metricValue prominently displayed, study.industry badge, "RomAIx Case Study" footer

IMPORTANT: Use simple CSS-in-JS style objects. ImageResponse does NOT support Tailwind classes. Use flexbox for layout. Keep fonts to system defaults or embed via fetch.
  </action>
  <verify>
Run `npm run dev`. Visit http://localhost:3000/opengraph-image - should render PNG image. Visit http://localhost:3000/blog/ai-agents-travel-industry/opengraph-image - should show blog post title. Visit http://localhost:3000/case-studies/adventure-tours/opengraph-image - should show case study with metric.
  </verify>
  <done>
Default OG image shows RomAIx branding. Blog post OG images show post title and category. Case study OG images show title and hero metric.
  </done>
</task>

</tasks>

<verification>
1. `npm run dev` - no errors
2. View page source on blog post - contains og:type="article" and og:published_time
3. View page source on case study - contains og:type="article"
4. Direct URL /opengraph-image returns PNG image
5. Direct URL /blog/[any-post]/opengraph-image returns unique PNG
6. Direct URL /case-studies/[any-study]/opengraph-image returns unique PNG
7. Share a blog post URL in LinkedIn post preview tool - shows custom image
</verification>

<success_criteria>
- Blog generateMetadata includes OpenGraph with article type and published time
- Case study generateMetadata includes OpenGraph with article type
- Default opengraph-image.tsx generates branded homepage image
- Blog opengraph-image.tsx generates unique images per post
- Case study opengraph-image.tsx generates unique images per study
- All OG images are 1200x630 PNG format
</success_criteria>

<output>
After completion, create `.planning/phases/08-seo-performance-deployment/08-02-SUMMARY.md`
</output>
